## [2025-01-19] - Скрытие блока синхронизации для неадминов в воркспейсе

### Исправлено
- **ПРАВА ДОСТУПА**: Блок синхронизации в меню действий воркспейса теперь скрыт для пользователей без прав администратора
- **БЕЗОПАСНОСТЬ**: Неадмины не видят функции синхронизации остатков
- **UX УЛУЧШЕНИЕ**: Интерфейс адаптируется под права пользователя

### Технические детали
- Добавлен ID `syncSection` к блоку синхронизации в `app/templates/catalog_new_blocks/workspace.html`
- Добавлена функция `hideSyncSectionForNonAdmins()` для скрытия блока
- Функция вызывается при инициализации воркспейса и при загрузке страницы
- Проверка прав доступа через элемент `user-data` с атрибутом `data-is-admin`
- Добавлено логирование для отладки скрытия блока синхронизации
- Теперь неадмины не видят кнопки "Включить/Выключить синхронизацию остатков"

## [2025-01-19] - Реализация персональных воркспейсов для каждого пользователя

### Добавлено
- **ПЕРСОНАЛЬНЫЕ ВОРКСПЕЙСЫ**: Каждый пользователь теперь имеет свой собственный воркспейс
- **ИЗОЛИРОВАННОЕ ХРАНЕНИЕ**: Воркспейсы сохраняются отдельно для каждого пользователя в localStorage
- **МИГРАЦИЯ ДАННЫХ**: Автоматическая миграция старых данных воркспейса на новую систему
- **ОТОБРАЖЕНИЕ ПОЛЬЗОВАТЕЛЯ**: В заголовке воркспейса отображается информация о текущем пользователе

### Технические детали
- Модифицирован класс `SimpleWorkspaceManager` в `app/templates/catalog_new_blocks/workspace.html`
- Добавлен метод `getUserEmail()` для получения email пользователя
- Ключ хранения изменен с `catalog_workspace` на `catalog_workspace_{userEmail}`
- Добавлена автоматическая миграция данных со старого ключа на новый
- Добавлен метод `updateUserInfo()` для отображения информации о пользователе
- Добавлено логирование для отладки работы с персональными воркспейсами
- Теперь каждый пользователь имеет изолированный воркспейс

## [2025-01-19] - Полное исправление ошибки ReferenceError в консоли браузера

### Исправлено
- **ОШИБКА КОНСОЛИ**: Полностью исправлена ошибка "ReferenceError: removeFromWorkspace is not defined" в консоли браузера
- **ГЕНЕРАЦИЯ HTML**: Исправлена генерация HTML для карточек товаров в воркспейсе
- **ОБРАБОТЧИК СОБЫТИЙ**: Улучшен обработчик кликов для кнопок удаления из воркспейса
- **ДИНАМИЧЕСКИЕ КНОПКИ**: Исправлена работа с динамически создаваемыми кнопками удаления

### Технические детали
- Убрана проверка на несуществующий `onclick` атрибут в `app/templates/catalog_new_blocks/workspace.html`
- Исправлена функция `modify_card_for_workspace()` в `app/api/v1/routers/products.py`
- Убран `onclick="removeFromWorkspace(this)"` и добавлен `data-sku` атрибут
- Улучшен способ получения SKU товара из кнопки удаления через `data-sku` атрибут
- Исправлен обработчик событий для корректной работы с динамически создаваемыми кнопками
- Теперь удаление товаров из воркспейса работает без ошибок в консоли

## [2025-01-19] - Предотвращение доступа к операциям со складом для неадминов

### Исправлено
- **ПРЕДВАРИТЕЛЬНАЯ ПРОВЕРКА**: Добавлена проверка прав доступа перед открытием модального окна перемещения
- **БЛОКИРОВКА ОПЕРАЦИЙ**: Пользователи без прав администратора не могут выполнять операции со складом
- **РАННЕЕ УВЕДОМЛЕНИЕ**: Модальное окно не открывается для неадминов, сразу показывается уведомление

### Технические детали
- Добавлен скрытый элемент с данными пользователя в `app/templates/catalog_new_blocks/product_card.html`
- Добавлена проверка прав доступа в функцию `openTransferModal()`
- Добавлена проверка прав доступа в функцию `removeFromStock()`
- Добавлена проверка прав доступа в функцию `addToStock()`
- Добавлена проверка прав доступа в функцию `deleteProduct()`
- Теперь неадмины получают уведомление о недостатке прав до открытия модального окна

## [2025-01-19] - Улучшение обработки ошибок при операциях со складом

### Исправлено
- **ОБРАБОТКА ОШИБОК**: Исправлена обработка ошибок при перемещении товаров между складами
- **ДЕТАЛЬНЫЕ СООБЩЕНИЯ**: Добавлены детальные сообщения об ошибках для всех операций со складом
- **ПРАВА ДОСТУПА**: Улучшено отображение ошибок для пользователей без прав администратора

### Технические детали
- Улучшена функция `confirmTransfer()` в `app/templates/catalog_new_blocks/product_card.html`
- Добавлена детальная обработка HTTP статус кодов (401, 403, 404, 400, 500)
- Улучшена функция `removeFromStock()` с детальными сообщениями об ошибках
- Улучшена функция `addToStock()` с детальными сообщениями об ошибках
- Улучшена функция `deleteProduct()` с детальными сообщениями об ошибках
- Теперь пользователи получают понятные сообщения об ошибках вместо общих фраз

## [2025-01-19] - Улучшение отладки загрузки изображений по HTTP ссылкам

### Добавлено
- **ДЕТАЛЬНОЕ ЛОГИРОВАНИЕ**: Добавлено подробное логирование процесса загрузки изображений по HTTP ссылкам
- **ОТЛАДКА URL**: Добавлена проверка на обрезанные или неполные URL в файлах импорта
- **МОНИТОРИНГ ПРОЦЕССА**: Добавлено логирование всех этапов: обнаружение ссылки, скачивание, обработка, сохранение

### Технические детали
- Улучшена функция `download_image_from_url()` в `app/services/warehouse/manager.py`
- Добавлено логирование в функцию `robust_import_incoming_from_excel()`
- Добавлена проверка на обрезанные URL (заканчивающиеся на "pr" или короче 20 символов)
- Логирование включает: URL, размер скачанного файла, статус обработки, результат сохранения
- Теперь можно отследить весь процесс загрузки изображений через логи

## [2025-01-19] - Исправление проблемы с перерисовкой карточек товаров после изменений

### Исправлено
- **ПЕРЕРИСОВКА КАРТОЧЕК**: Исправлена проблема с пропаданием изображений при перерисовке карточек товаров после изменений
- **AJAX ЭНДПОИНТЫ**: Добавлено поле `has_image` во все AJAX эндпоинты для получения карточек товаров
- **СИНХРОНИЗАЦИЯ ДАННЫХ**: Обеспечена синхронизация данных между основным каталогом и AJAX обновлениями

### Технические детали
- Исправлены эндпоинты в `app/api/v1/routers/products.py`:
  - `/catalog_new/api/products/{sku}/card` - получение одной карточки
  - `/catalog_new/api/products/cards` - получение массива карточек
  - `/catalog_new/api/products/workspace-cards` - получение карточек для воркспейса
- Исправлен эндпоинт в `app/api/v1/routers/catalog.py`:
  - `/catalog_new/api/products/{sku}/card` - получение карточки для нового каталога
- Добавлено поле `"has_image": bool(product.original_image)` во все AJAX ответы
- Теперь изображения корректно отображаются при обновлении карточек через AJAX

## [2025-01-19] - Исправление проблемы с отображением изображений из HTTP ссылок

### Исправлено
- **ОТОБРАЖЕНИЕ ИЗОБРАЖЕНИЙ**: Исправлена проблема с отображением изображений товаров, загруженных по HTTP ссылкам
- **ЛОГИКА ПРОВЕРКИ**: Изменена логика проверки наличия изображений в шаблонах с `product['image']` на `product['has_image']`
- **ОБРАБОТКА ИЗОБРАЖЕНИЙ**: Исправлен вызов функции `compress_image()` с правильными параметрами SKU

### Технические детали
- Исправлена функция `robust_import_incoming_from_excel()` в `app/services/warehouse/manager.py`
- Исправлена функция `import_incoming_from_excel()` в `app/services/warehouse/manager.py`
- Добавлено поле `has_image` в API ответы каталога и продуктов
- Обновлен шаблон `product_card.html` для использования правильной проверки изображений
- Теперь изображения корректно отображаются при импорте по HTTP ссылкам

## [2025-01-19] - Реализация надежной системы импорта прихода с полным отчетом

### Добавлено
- **НАДЕЖНЫЙ ИМПОРТ**: Создана новая функция `robust_import_incoming_from_excel()` для стабильного импорта прихода
- **ПОЛНЫЙ ОТЧЕТ**: Каждая строка файла обрабатывается и включается в детальный отчет с результатом операции
- **ПРОВЕРКА СУЩЕСТВОВАНИЯ**: Система проверяет существование товара в базе и выбирает соответствующую стратегию
- **ОТЧЕТ В EXCEL**: Автоматическая генерация Excel файла с отчетом об импорте и статистикой
- **ОБРАБОТКА ОШИБОК**: Все ошибки фиксируются в отчете с подробным описанием проблемы

### Изменено
- **РОУТ ИМПОРТА**: Обновлен роут `/api/warehouse/incoming/` для использования новой логики
- **ЛОГИКА ОБРАБОТКИ**: Изменена стратегия обработки файлов - теперь каждая строка обрабатывается независимо
- **ВОЗВРАТ ДАННЫХ**: Роут теперь возвращает файл отчета вместо JSON ответа

### Технические детали
- Создана функция `robust_import_incoming_from_excel()` в `app/services/warehouse/manager.py`
- Добавлена функция `generate_import_report_excel()` для генерации отчетов
- Обновлен роут импорта прихода в `app/api/v1/routers/warehouse.py`
- Отчет включает два листа: "Отчет импорта" и "Статистика"
- Цветовое кодирование: зеленый для успешных операций, красный для ошибок
- Статистика показывает общее количество обработанных строк и процент успеха

## [2025-01-19] - Добавление шаблона для импорта перемещений между складами

### Добавлено
- **ШАБЛОН ПЕРЕМЕЩЕНИЯ**: Создан новый роут `/api/warehouse/template/transfer` для скачивания шаблона Excel для импорта перемещений между складами
- **КНОПКА ШАБЛОНА**: Добавлена кнопка "📥 Скачать шаблон" в форму импорта перемещения на странице операций
- **ИНСТРУКЦИИ**: Шаблон включает подробные инструкции по использованию и примеры данных

### Технические детали
- Создан роут `download_transfer_template()` в `app/api/v1/routers/warehouse.py`
- Шаблон содержит колонки: `sku` (обязательно) и `Кол-во` (обязательно)
- Добавлен лист с инструкциями для пользователей
- Обновлен файл `app/templates/operations.html`: добавлена кнопка скачивания шаблона
- Шаблон генерируется динамически с примерами данных и форматированием

## [2025-01-19] - Обновление базового шаблона для перенаправления на новый каталог

### Изменено
- **ОБНОВЛЕНИЕ НАВИГАЦИИ**: Все ссылки в базовом шаблоне теперь ведут на новый каталог `/catalog_new`
- **ЛОГОТИП**: Ссылка логотипа обновлена с `/catalog` на `/catalog_new`
- **ГЛАВНОЕ МЕНЮ**: Ссылка "Склад" в десктопном меню обновлена на новый каталог
- **МОБИЛЬНОЕ МЕНЮ**: Ссылка "Склад" в мобильном меню обновлена на новый каталог
- **ГЛАВНАЯ СТРАНИЦА**: Перенаправление с главной страницы теперь ведет на `/catalog_new`
- **СТАТИСТИКА**: Ссылки на статистику товаров обновлены для нового каталога
- **ФИЛЬТРЫ**: Ссылка на товары с низким остатком обновлена с параметрами фильтрации

### Технические детали
- Обновлен файл `app/templates/base.html`: изменены все ссылки с `/catalog` на `/catalog_new`
- Обновлен файл `app/main.py`: изменено перенаправление с главной страницы
- Обновлен файл `app/templates/index.html`: изменены ссылки в статистике
- Сохранена обратная совместимость: старый каталог остается доступным по старым маршрутам

## [2025-01-19] - Очистка избыточного логирования в шаблонах

### Исправлено
- **ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ**: Удалено избыточное логирование в консоль из шаблонов
- **ОЧИСТКА SCRIPTS.HTML**: Убраны отладочные сообщения из функций обновления карточек
- **ОЧИСТКА PRODUCT_CARD.HTML**: Удалены избыточные логи из drag-and-drop обработчиков и модальных окон
- **ОЧИСТКА WORKSPACE.HTML**: Убраны отладочные сообщения из функций воркспейса
- **СОХРАНЕНИЕ КРИТИЧЕСКИХ ЛОГОВ**: Оставлены только важные сообщения об ошибках для отладки
- **УЛУЧШЕНИЕ ЧИТАЕМОСТИ**: Код стал более чистым и производительным

### Технические детали
- Удалены console.log сообщения из функций refreshProductCards, refreshCatalogCard, refreshWorkspaceCard
- Очищены отладочные логи из drag-and-drop обработчиков и модальных окон
- Убраны избыточные логи из функций highlightSelectedProducts, renderWorkspaceProducts, showNotification
- Сохранены только console.error для критических ошибок
- Улучшена производительность за счет уменьшения количества операций логирования

## [2025-01-19] - Исправление дублирования карточек при обновлении

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Устранено дублирование карточек товаров при обновлении в каталоге
- **ПРОБЛЕМА ВЛОЖЕННОСТИ**: Исправлена логика обновления карточек в функции `refreshCatalogCard`
- **ПРАВИЛЬНОЕ ИЗВЛЕЧЕНИЕ СОДЕРЖИМОГО**: Теперь извлекается только внутреннее содержимое карточки (без внешнего div.product-card)
- **КОНСИСТЕНТНОСТЬ**: Применено одинаковое исправление для обновления карточек в основном каталоге и правой панели воркспейса
- **ОТЛАДКА**: Улучшено логирование процесса обновления карточек для лучшей диагностики

### Технические детали
- Изменена логика в строках 914-923: теперь используется `newCardContent.innerHTML` вместо `cardToUse.innerHTML`
- Добавлена аналогичная логика для правой панели воркспейса (строки 953-962)
- Сохранен fallback механизм для случаев, когда структура HTML неожиданная

## [2025-01-19] - Улучшение UX полей ввода количества и исправление модального окна

### Добавлено
- **УЛУЧШЕНИЕ UX**: Автоматическая очистка полей ввода количества при клике и возврат к 0 при потере фокуса
- Создана функция `initializeInputHandlers()` для управления поведением полей ввода
- Добавлена защита от повторной инициализации обработчиков через атрибут `data-input-initialized`
- Автоматическая переинициализация обработчиков после обновления карточек товаров
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Добавлено делегирование событий для динамически загружаемых полей ввода
- Глобальная функция `window.initializeInputHandlers()` для принудительной инициализации обработчиков
- Интеграция инициализации обработчиков в функции обновления карточек (`refreshProductCards`, `refreshWorkspaceCard`)

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Полностью устранена ошибка "Uncaught InternalError: too much recursion"
- **РАДИКАЛЬНОЕ УПРОЩЕНИЕ**: Полностью удалена функция initializeInputHandlers и все ее вызовы
- **ТОЛЬКО ДЕЛЕГИРОВАНИЕ**: Оставлено только делегирование событий для обработки полей ввода
- **КОНФЛИКТ ПЕРЕМЕННЫХ**: Исправлен конфликт переменных transferModalWrapper (изменено с let на var)
- **ПРОБЛЕМА С ДАННЫМИ DRAG-AND-DROP**: Исправлена передача пустых данных при перетаскивании в блоке каталога
- **РЕЗЕРВНЫЙ МЕХАНИЗМ**: Добавлен fallback механизм для получения данных из глобальной переменной
- **РЕФАКТОРИНГ АРХИТЕКТУРЫ**: Заменен outerHTML на innerHTML для сохранения обработчиков событий
- **СОВРЕМЕННЫЙ ПОДХОД**: Реализовано делегирование событий для drag-and-drop вместо привязки к конкретным элементам
- **ЦЕНТРАЛИЗОВАННОЕ СОСТОЯНИЕ**: Создан глобальный объект window.dragState для управления состоянием drag-and-drop
- **ИСПРАВЛЕНИЕ ИНИЦИАЛИЗАЦИИ МОДАЛЬНОГО ОКНА**: Исправлена ошибка "Transfer modal elements not found"
- **ПРЕДОТВРАЩЕНИЕ КЛИКА ПО СКЛАДАМ**: Добавлено предотвращение клика по блокам складов для добавления в воркспейс
- **ПРЕДОТВРАЩЕНИЕ КЛИКА ПО СЕКЦИИ ОСТАТКОВ**: Добавлено предотвращение клика по всей секции "Остатки по складам"
- **ГЛОБАЛЬНЫЕ УВЕДОМЛЕНИЯ**: Модальное окно ошибок теперь создается динамически на уровне document.body
- **ИСПРАВЛЕНИЕ КНОПОК**: Исправлена работа кнопок "Списать" и "Пополнить" на карточках товаров
- **ИСПРАВЛЕНИЕ КОНФЛИКТА ПРЕДОТВРАЩЕНИЯ**: Исправлен конфликт между предотвращением клика по складам и работой кнопок управления остатками
- **ОБЪЕДИНЕНИЕ ОБРАБОТЧИКОВ**: Объединены обработчики событий для кнопок и предотвращения клика в один с правильным приоритетом
- **ОТЛАДКА ОПРЕДЕЛЕНИЯ КНОПОК**: Добавлена отладочная информация и альтернативные способы определения кнопок
- **ИСПРАВЛЕНИЕ ДВОЙНОГО ОБНОВЛЕНИЯ**: Исправлена проблема двойного увеличения количества при пополнении/списании
- **ОТЛАДКА ПРОБЛЕМЫ СПИСАНИЯ**: Добавлено детальное логирование для диагностики проблем с неправильным списанием
- **ИСПРАВЛЕНИЕ КАРТОЧКИ В КАРТОЧКЕ**: Исправлена проблема отрисовки карточки внутри карточки при обновлении
- **ЗАЩИТА ОТ МНОЖЕСТВЕННЫХ ВЫЗОВОВ**: Добавлена защита от одновременных вызовов refreshProductCards
- **ИСПРАВЛЕНИЕ ПОВТОРНОГО ОТКРЫТИЯ МОДАЛЬНОГО ОКНА**: Исправлена проблема с повторным открытием модального окна после закрытия
- **УЛУЧШЕНИЕ ОТЛАДКИ КАРТОЧКИ В КАРТОЧКЕ**: Добавлена детальная отладка для диагностики проблемы карточки в карточке
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Исправлено отображение модального окна перемещения товаров между складами в контексте воркспейса
- **ПРОБЛЕМА С РАЗДЕЛЕННЫМ ИНТЕРФЕЙСОМ**: Модальное окно теперь создается динамически на уровне document.body вместо статического HTML
- Обновлен z-index модального окна с z-[9999] до z-index: 99999 !important для корректного отображения поверх всех элементов
- Добавлена принудительная установка стилей позиционирования (position: fixed, top: 0, left: 0, width: 100vw, height: 100vh)
- Исправлена проблема с пустыми данными drag-and-drop в консоли браузера
- Модальное окно теперь корректно отображается по центру экрана поверх обоих блоков (воркспейс и каталог)
- Добавлена расширенная отладочная информация для диагностики проблем с модальными окнами
- Исправлено сброс стилей при закрытии модального окна

### Технические детали
- **Радикальное упрощение**: Полностью удалена функция `initializeInputHandlers()` и глобальная функция `window.initializeInputHandlers()`
- **Только делегирование**: Все обработчики полей ввода работают исключительно через делегирование событий
- **Делегирование событий**: Использование capture phase для обработки событий динамически загружаемых элементов
- **Исправление конфликтов**: Изменение объявления переменных с `let` на `var` для предотвращения конфликтов
- **Резервный механизм данных**: Добавлена глобальная переменная `window.lastDragData` для сохранения данных drag-and-drop
- **Fallback для пустых данных**: При получении пустых данных из `dataTransfer` используется резервный механизм
- **Архитектурный рефакторинг**: Замена `outerHTML` на `innerHTML` для сохранения DOM элементов и их обработчиков
- **Современный подход к drag-and-drop**: Полное делегирование событий вместо привязки к конкретным элементам
- **Централизованное состояние**: Глобальный объект `window.dragState` для управления состоянием перетаскивания
- **Упрощение логики**: Удалены старые функции `setupDragAndDropForItems` и `initializeDragAndDrop`
- **Динамическое создание модального окна**: Модальное окно создается автоматически при первом вызове `initializeTransferModal()`
- **Исправление порядка инициализации**: Убраны преждевременные вызовы инициализации модального окна
- **Предотвращение клика по складам**: Добавлена проверка в функции `addToWorkspaceFromCard` для блоков `.warehouse-item`
- **Делегированное предотвращение**: Добавлен делегированный обработчик кликов для предотвращения добавления в воркспейс при клике по складам
- **Предотвращение клика по секции остатков**: Добавлен класс `.stocks-section` для идентификации секции "Остатки по складам"
- **Полное предотвращение**: Предотвращение клика работает как для отдельных блоков складов, так и для всей секции остатков
- **Динамическое создание модального окна ошибок**: Функция `createErrorModalIfNeeded()` создает модальное окно ошибок на уровне document.body
- **Глобальная функция уведомлений**: `window.showErrorModal()` теперь работает с динамически созданным модальным окном
- **Единообразный интерфейс**: Все модальные окна (перемещение и ошибки) создаются динамически с одинаковым z-index
- **Инициализация при загрузке**: Модальное окно ошибок создается сразу при загрузке страницы через `createErrorModalIfNeeded()`
- **Исправление порядка инициализации**: Функция `showErrorModal` теперь доступна с самого начала работы страницы
- **Делегированные обработчики кнопок**: Добавлены делегированные обработчики событий для кнопок "Списать" и "Пополнить"
- **Исправление работы с динамическим контентом**: Кнопки управления остатками теперь работают с динамически загружаемыми карточками товаров
- **Исключения для кнопок**: Добавлены исключения для кнопок "Списать" и "Пополнить" в предотвращении клика по блокам складов
- **Исключения для полей ввода**: Добавлены исключения для полей ввода количества в предотвращении клика по блокам складов
- **Приоритет обработчиков**: Кнопки управления остатками имеют приоритет над предотвращением клика по складам
- **Единый обработчик событий**: Все логика обработки кликов объединена в один обработчик с четким приоритетом
- **Правильный порядок выполнения**: Кнопки обрабатываются первыми, затем поля ввода, затем предотвращение клика
- **Исключение конфликтов**: Использование return предотвращает выполнение последующих проверок
- **Отладочная информация**: Добавлено логирование всех кликов для диагностики проблем
- **Альтернативные способы определения**: Добавлена проверка через onclick атрибут как резервный метод
- **Улучшенная проверка текста**: Добавлена проверка с trim() для обработки пробелов
- **Устранение двойного обновления**: Убрано локальное обновление интерфейса, оставлено только серверное
- **Единый источник истины**: Все обновления количества теперь происходят только через сервер
- **Упрощение логики**: Функции addToStock и removeFromStock теперь только отправляют запросы на сервер
- **Детальное логирование**: Добавлено логирование параметров списания/пополнения для диагностики
- **Счетчик вызовов**: Добавлен счетчик вызовов refreshProductCards для отслеживания множественных обновлений
- **Отладочная информация**: Логирование текущего количества на складе и параметров операций
- **Извлечение содержимого карточки**: Исправлено обновление карточки через извлечение innerHTML вместо полной замены
- **Флаг выполнения**: Добавлен флаг window.refreshInProgress для предотвращения множественных одновременных обновлений
- **Автоматическая очистка**: Флаг выполнения автоматически очищается в блоке finally
- **Сброс флагов при закрытии**: Флаг window.transferModalInitialized сбрасывается при удалении модального окна из DOM
- **Повторное создание модального окна**: После закрытия модальное окно может быть создано заново
- **Автоматический сброс состояния**: Состояние инициализации автоматически сбрасывается при удалении элементов
- **Детальная отладка обновления карточек**: Добавлено логирование содержимого data.html и процесса извлечения карточки
- **Альтернативные способы поиска**: Добавлены альтернативные селекторы для поиска карточки в HTML
- **Улучшенная логика извлечения**: Используется лучший доступный способ извлечения содержимого карточки
- **Поведение полей ввода**: При клике поле очищается, при потере фокуса возвращается к 0
- **Динамическое создание**: Модальное окно создается через `createTransferModalIfNeeded()` и добавляется в `document.body`
- **Автоматическое удаление**: Модальное окно удаляется из DOM при закрытии для предотвращения накопления элементов
- Принудительная установка z-index: 99999 через inline стили
- Полное позиционирование модального окна через JavaScript
- Дополнительная проверка видимости модального окна через getBoundingClientRect()
- Корректный сброс всех принудительно установленных стилей при закрытии

## [2025-01-19] - Исправление ошибки JSON.parse в drag-and-drop

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Исправлена ошибка `SyntaxError: JSON.parse: unexpected end of data` в обработчиках drop
- Добавлена проверка на пустые данные перед парсингом JSON в глобальном и обычном обработчиках drop
- Улучшена обработка ошибок с детальным логированием проблемных данных
- Добавлена верификация данных drag-and-drop в dragstart событии
- Исправлена проблема с передачей данных между dragstart и drop событиями

## [2025-01-19] - Исправление видимости модального окна drag-and-drop

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Исправлена проблема с невидимым модальным окном при drag-and-drop
- Увеличен z-index модального окна с z-50 до z-[9999] для корректного отображения поверх воркспейса
- Добавлена автоматическая инициализация модального окна при его отсутствии
- Добавлена расширенная отладка состояния модального окна
- Исправлена проблема с drag-and-drop в контексте открытого воркспейса

### Добавлено
- **Повышенный z-index**: Модальное окно теперь отображается поверх всех элементов интерфейса
- **Автоматическая инициализация**: Модальное окно инициализируется автоматически при необходимости
- **Расширенная отладка**: Детальное логирование состояния модального окна
- **Глобальные обработчики**: Специальные обработчики drag-and-drop для работы в воркспейсе

## [2025-01-19] - Исправление бесконечной рекурсии в drag-and-drop

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Устранена бесконечная рекурсия между `initializeDragAndDrop` и `reinitializeDragAndDrop`
- Добавлен флаг `window.dragDropInitialized` для предотвращения повторных вызовов
- Переписана функция `reinitializeDragAndDrop` для прямого вызова логики без рекурсии
- Автоматический сброс флага через 1 секунду после завершения инициализации

### Добавлено
- **Защита от рекурсии**: Проверка флага перед вызовом функции
- **Прямая инициализация**: Логика drag-and-drop вызывается напрямую без промежуточных функций
- **Автоматический сброс**: Флаг сбрасывается автоматически для возможности повторной инициализации

### Технические детали
- Флаг `window.dragDropInitialized` предотвращает повторные вызовы
- Функция `reinitializeDragAndDrop` содержит всю логику инициализации
- setTimeout для сброса флага через 1000ms
- Улучшенное логирование для отслеживания процесса

## [2025-01-19] - Исправление интеграции drag-and-drop с scripts.html

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Проблема с вызовом функции drag-and-drop из scripts.html
- В scripts.html была заглушка функции `initializeDragAndDrop`, которая не вызывала реальную функциональность
- Добавлен вызов `window.reinitializeDragAndDrop()` из заглушки в scripts.html
- Исправлена интеграция между scripts.html и product_card.html

### Добавлено
- **Интеграция с scripts.html**: Заглушка теперь вызывает реальную функцию drag-and-drop
- **Проверка доступности**: Проверка существования функции перед вызовом
- **Улучшенное логирование**: Различение вызовов из разных файлов

### Технические детали
- Обновлена заглушка в `scripts.html` для вызова `window.reinitializeDragAndDrop()`
- Добавлены отличительные логи для отслеживания источника вызова
- Проверка `typeof window.reinitializeDragAndDrop === 'function'` перед вызовом

## [2025-01-19] - Улучшение инициализации drag-and-drop функциональности

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Проблема с инициализацией drag-and-drop для динамически загружаемых элементов
- Добавлена задержка инициализации для корректной работы с динамическим контентом
- Улучшена логика поиска элементов складов с fallback на `[data-warehouse]`
- Автоматическое добавление класса `warehouse-item` к элементам с `data-warehouse`

### Добавлено
- **Улучшенная диагностика**: Расширенное логирование процесса инициализации
- **Глобальная функция**: `window.reinitializeDragAndDrop()` для повторной инициализации
- **Автоматическое исправление**: Добавление недостающих классов к элементам складов
- **Fallback механизм**: Поиск элементов по атрибутам, если классы не найдены

### Технические детали
- Разделение логики на `initializeDragAndDrop()` и `setupDragAndDropForItems()`
- Добавление setTimeout(100ms) для корректной работы с динамическим контентом
- Проверка существования элементов с детальным логированием
- Автоматическое исправление структуры DOM при необходимости

## [2025-01-19] - Исправление drag-and-drop функциональности для перемещения товаров между складами

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Исправлена проблема с областью видимости функций в drag-and-drop коде
- Исправлена ошибка с переменной `handleKeydown` в функции `openTransferModal`
- Добавлены глобальные переменные для элементов модального окна
- Добавлена проверка инициализации всех элементов модального окна
- Добавлено подробное логирование для диагностики проблем

### Добавлено
- **Отладочная информация**: Подробные console.log для отслеживания процесса drag-and-drop
- **Проверки элементов**: Валидация существования всех необходимых DOM элементов
- **Улучшенная обработка ошибок**: Более детальные сообщения об ошибках

## [2025-01-19] - Реализация drag-and-drop функциональности для перемещения товаров между складами

### Добавлено
- **Новая drag-and-drop функциональность**: Заменена кнопка "Переместить" на интуитивное перетаскивание элементов складов
- **Модальное окно подтверждения**: При перетаскивании склада на другой склад открывается модальное окно с предзаполненными данными
- **Валидация количества**: Обязательное поле количества с ограничениями (минимум 1, максимум - остаток на складе-источнике)
- **Улучшенный UX**: 
  - Визуальные эффекты при перетаскивании (поворот, прозрачность, подсветка)
  - Подсказка "Отпустите для перемещения" при наведении на целевой склад
  - Анимация появления модального окна
  - Автофокус на поле количества

### Технические детали
- **HTML изменения**: 
  - Добавлен атрибут `draggable="true"` к элементам складов
  - Добавлены data-атрибуты для передачи информации о складе
  - Добавлена иконка перетаскивания (две горизонтальные линии)
- **CSS стили**:
  - Стили для состояний `dragging`, `drag-over`
  - Анимации и переходы для плавного UX
  - Стили для модального окна с backdrop-filter
- **JavaScript функциональность**:
  - Обработчики событий `dragstart`, `dragend`, `dragover`, `dragleave`, `drop`
  - Предотвращение перетаскивания при клике на интерактивные элементы
  - Валидация: нельзя перетаскивать пустые склады или в тот же склад
  - Обработка клавиши Enter для подтверждения
  - Закрытие модалки по Escape, клику вне модалки или кнопке "Отмена"

### API интеграция
- **Новый endpoint**: `/api/warehouse/transfer/` для перемещения товаров между складами
- **Параметры запроса**: `sku`, `from_warehouse`, `to_warehouse`, `quantity`
- **Уведомления**: Показ успешного сообщения после перемещения
- **Обновление UI**: Автоматическое обновление карточек товаров после перемещения

## [2025-01-19] - Исправление ошибки с атрибутом 'success' в синхронизации офферов

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Исправлена ошибка `'dict' object has no attribute 'success'` в `sync_tasks.py`
- Проблема была в несоответствии между возвращаемым типом метода `update_stock_single` и ожидаемой структурой данных
- Создана правильная модель ответа `SingleTokenStockUpdateResponse` для эндпоинта `/update-stock-single`

### Добавлено
- **Новая модель ответа**: `SingleTokenStockUpdateResponse` в `app/models/offer.py` с полями:
  - `token_id`, `account_name`, `external_id`, `requested_stock`
  - `offers` (список результатов обновления офферов)
  - `total_offers`, `updated_offers`, `success`
- **Модель результата**: `OfferUpdateResult` для детализации результатов обновления каждого оффера
- **Обновленный метод**: `update_stock_single()` теперь возвращает `SingleTokenStockUpdateResponse` вместо `MicroserviceOfferResponse`

### Технические детали
- Метод корректно обрабатывает ответ от эндпоинта `/update-stock-single`
- Код в `sync_tasks.py` теперь правильно получает атрибут `success` из ответа
- Полная типизация с использованием Pydantic моделей
- Поддержка детальной информации об обновлении каждого оффера

## [2025-01-19] - Исправление проблем с синхронизацией офферов Allegro

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ 1**: Исправлена логика обработки ответов микросервиса Allegro
  - Проблема была в том, что код пытался найти `external.id` в корневом объекте ответа микросервиса, а не в самих офферах внутри массива `offers`
  - Теперь код правильно извлекает массив офферов из поля `offers` ответа микросервиса и обрабатывает каждый оффер отдельно

- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ 2**: Исправлен endpoint для обновления остатков офферов
  - Изменен с `/api/v1/offers/token/{token_id}/offer/{offer_id}/stock` на `/api/v1/offers/update-stock`
  - Обновлена логика передачи параметров: теперь используется `external_id` (SKU) вместо `offer_id`
  - Добавлен параметр `token_ids` в query string как список UUID согласно API документации

### Добавлено
- Детальное логирование структуры ответов микросервиса и офферов для диагностики
- Логирование количества найденных офферов в каждом ответе микросервиса
- Улучшенное логирование процесса обработки офферов с указанием номера ответа и оффера

## [2025-01-19] - Исправление проблемы с обновлением карточек при пополнении

### Исправлено
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Функция `refreshProductCards` не была доступна на странице каталога, так как определялась только в `workspace.html`. Добавлена в `scripts.html` для доступности на всех страницах
- Исправлен метод обновления карточек: заменен `replaceChild` на `outerHTML` для более надежного обновления DOM
- Добавлено подробное логирование для диагностики процесса обновления карточек
- **Исправлена синхронизация выделения товаров**: добавлен вызов `workspaceManager.highlightSelectedProducts()` после обновления карточек в функциях `refreshProductCards`, `refreshCatalogCard` и `refreshWorkspaceCard`
- Теперь после пополнения/списания товара карточки обновляются с сохранением визуального выделения в соответствии с содержимым воркспейса

### Добавлено
- Функции `refreshProductCards`, `refreshCatalogCard`, `refreshWorkspaceCard` добавлены в `scripts.html`
- Подробное логирование с эмодзи для отслеживания процесса обновления
- Проверка наличия карточек на странице перед обновлением

## [2025-01-19] - Унификация шаблонов карточек товаров и улучшение системы обновления

### Добавлено
- **Унифицированный подход**: Теперь используется один шаблон `product_card.html` для всех карточек товаров
- Новый API endpoint `/catalog_new/api/cards` для получения массива карточек с поддержкой контекста
- Функция `modify_card_for_workspace()` для динамической модификации карточек для воркспейса
- Поддержка параметра `context` в API endpoints (`catalog` или `workspace`)
- Универсальная функция `refreshProductCards(sku)` для обновления всех карточек товара
- Функция `refreshCatalogCard(sku)` для обновления карточек в каталоге
- Функция `refreshWorkspaceCard(sku)` для обновления карточек в воркспейсе
- Автоматический вызов обновления карточек после успешных операций пополнения/списания
- Подробное логирование для отладки процесса обновления карточек

### Изменено
- **Архитектурное изменение**: Удален отдельный шаблон `workspace_product_card.html`
- Все карточки теперь рендерятся из единого шаблона `product_card.html`
- Для воркспейса карточки модифицируются динамически: удаляется `onclick` и добавляется кнопка удаления
- Все функции `removeFromStock()` и `addToStock()` теперь автоматически обновляют карточки товаров после успешных операций
- Обновление происходит как в основном каталоге, так и в воркспейсе (если товар там присутствует)
- Функция `refreshProductCards()` доступна глобально через `window.refreshProductCards`
- Добавлено подробное логирование с эмодзи для отслеживания процесса обновления
- **Удален `draggable`** из всех карточек товаров

### Исправлено
- Критическая ошибка: кнопки "Списать" и "Пополнить" в карточках воркспейса вызывали удаление товара из воркспейса
- Улучшен селектор в глобальном обработчике кликов для более точного определения кнопки удаления из воркспейса
- Добавлен параметр `event` в функции `removeFromStock()` и `addToStock()` для предотвращения всплытия событий
- Обновлены все вызовы функций управления складом для передачи события
- Исправлены селекторы в JavaScript коде для поиска кнопок с обновленными обработчиками

## [2025-01-19] - Автоматическое обновление карточек товаров после операций со складом

### Добавлено
- Универсальная функция `refreshProductCards(sku)` для обновления всех карточек товара
- Функция `refreshCatalogCard(sku)` для обновления карточек в каталоге
- Функция `refreshWorkspaceCard(sku)` для обновления карточек в воркспейсе
- Автоматический вызов обновления карточек после успешных операций пополнения/списания
- Подробное логирование для отладки процесса обновления карточек
- Функция `testRefreshCards(sku)` для тестирования обновления карточек из консоли браузера

### Изменено
- Все функции `removeFromStock()` и `addToStock()` теперь автоматически обновляют карточки товаров после успешных операций
- Обновление происходит как в основном каталоге, так и в воркспейсе (если товар там присутствует)
- Функция `refreshProductCards()` доступна глобально через `window.refreshProductCards`
- Добавлено подробное логирование с эмодзи для отслеживания процесса обновления

### Исправлено
- Критическая ошибка: кнопки "Списать" и "Пополнить" в карточках воркспейса вызывали удаление товара из воркспейса
- Улучшен селектор в глобальном обработчике кликов для более точного определения кнопки удаления из воркспейса
- Добавлен параметр `event` в функции `removeFromStock()` и `addToStock()` для предотвращения всплытия событий
- Обновлены все вызовы функций управления складом для передачи события
- Исправлены селекторы в JavaScript коде для поиска кнопок с обновленными обработчиками

## [2025-01-19] - Реализация удаления товаров из воркспейса через кнопку крестика

### Добавлено
- Создан отдельный шаблон `workspace_product_card.html` для карточек воркспейса
- Добавлена кнопка крестика в левый верхний угол карточек воркспейса
- Функция `removeFromWorkspace()` для удаления товаров из воркспейса
- Функция `addToWorkspaceFromCard()` для toggle функциональности в воркспейсе

### Изменено
- Оригинальные карточки каталога теперь работают как toggle (добавление/удаление)
- Удаление из воркспейса происходит через кнопку крестика в карточках воркспейса
- Исправлена ошибка с переменной `cards_html` в роуте `get_workspace_cards`
- Обновлена версия скрипта карточки товара до v12

## [2025-01-19] - Исправление ошибок с токенами Allegro

### Исправлено
- Критическая логическая ошибка в условии проверки токенов Allegro в products.py
- Исправлено условие `if not tokens_response.total == 0 or not tokens_response.items:` на `if tokens_response.total == 0 or not tokens_response.items:`
- Исправлены все 6 мест в коде где была эта ошибка
- Ошибка `AttributeError: 'dict' object has no attribute 'account_name'` в функции `manage_product`
- Исправлено обращение к полям токена: `token.account_name` → `token.get('account_name')`
- Исправлено обращение к полям токена: `token.token_id` → `token.get('id', '')`
- Исправлено поле токена с `token_id` на `id` в соответствии с реальной структурой данных
- Ошибка `AttributeError: 'Settings' object has no attribute 'ALLEGRO_MICROSERVICE_URL'` в функции `get_product_offers`
- Исправлено использование настройки: `settings.ALLEGRO_MICROSERVICE_URL` → `settings.MICRO_SERVICE_URL`

### Изменено
- Логика проверки токенов теперь работает правильно: если total равен 0 ИЛИ items пустой, то токены не найдены
- Убран неправильный `await` из вызовов синхронной функции `get_tokens()` в AllegroTokenMicroserviceClient
- Токены теперь корректно обрабатываются как словари (dict) вместо объектов с атрибутами
- Все вызовы `create_access_token()` используют правильный аргумент `user_id` вместо `data`
- Исправлено использование правильного названия настройки для URL микросервиса

### Результат
- Страница управления товаром теперь корректно показывает аккаунты Allegro вместо сообщения "Аккаунты Allegro не найдены"
- Устранены ошибки AttributeError при обработке данных токенов
- Исправлены ошибки с неправильными аргументами в `create_access_token()`
- Исправлена ошибка с неправильным названием настройки микросервиса

## [2025-01-19] - Улучшение UX полей ввода количества в карточках товаров

### Добавлено
- CSS стили для скрытия стрелок у полей ввода числа (spinner controls)
- Автоматическая очистка поля ввода при фокусе для удобства ввода
- Сброс значения поля ввода в 0 после выполнения операций списания и пополнения

### Изменено
- При фокусе на поле ввода оно автоматически очищается (стimage.pngановится пустым)
- Версия карточки товара обновлена до v10 для принудительного обновления кэша
- Улучшена валидация ввода - теперь позволяет пустые поля, валидация происходит при нажатии кнопки
- Улучшены сообщения об ошибках валидации с указанием минимального значения

### Исправлено
- Предупреждение линтера о совместимости CSS свойства `appearance`

## [2025-01-19] - Исправление проблемы с выделением карточки при клике на интерактивные элементы

### Исправлено
- Проблема с выделением карточки товара при клике на поля ввода количества
- Проблема с выделением карточки при клике на кнопки управления (списать, пополнить, редактировать, удалить)
- Проблема с выделением карточки при клике на кнопки синхронизации Allegro
- Проблема с выделением карточки при клике на изображение товара
- Проблема с выделением карточки при клике на SKU для копирования

### Добавлено
- `event.stopPropagation()` для всех интерактивных элементов в карточке товара
- Обработчики событий `click`, `mousedown` и `focus` для предотвращения всплытия событий
- Автоматическое добавление обработчиков для всех элементов с классами `input[type="number"]`, `button`, `.cursor-pointer`, `[onclick]`, `[data-sku]`

### Изменено
- Версия карточки товара обновлена до v8 для принудительного обновления кэша
- Все кнопки и интерактивные элементы теперь корректно предотвращают выделение карточки

## [2025-01-19] - Реализация API endpoints для действий воркспейса

### Добавлено
- **API endpoints для синхронизации**:
  - `/api/workspace/sync/enable` - включение синхронизации остатков для выбранных товаров через Allegro микросервис
  - `/api/workspace/sync/disable` - отключение синхронизации остатков для выбранных товаров через Allegro микросервис
- **API endpoints для заявок**:
  - `/api/workspace/transfer-request` - создание файла заявки на перемещение
  - `/api/workspace/supplier-request` - создание файла заявки поставщику
- Интеграция функций экспорта с существующими endpoints из warehouse.py
- Полная интеграция JavaScript функций с реальными API endpoints
- Автоматическое скачивание файлов при экспорте и создании заявок
- Обработка ошибок и показ соответствующих уведомлений пользователю

### Изменено
- Функции экспорта теперь используют существующие endpoints `/export/stock-with-sales/` и `/export/stock-with-sales/no-images/`
- Функции синхронизации теперь работают через Allegro микросервис и ProductAllegroSyncService
- Синхронизация создает/обновляет настройки для всех активных аккаунтов Allegro
- Все функции действий теперь выполняют реальные API вызовы вместо заглушек
- Добавлена асинхронная обработка всех операций с показом прогресса
- Улучшена обработка ошибок с детальными сообщениями для пользователя
- Файлы экспорта теперь в формате XLSX с изображениями и статистикой продаж

## [2025-01-19] - Добавление бургер-меню действий в воркспейс

### Добавлено
- Бургер-меню действий рядом с кнопкой очистки в заголовке воркспейса
- Выпадающее меню с четырьмя основными разделами действий:
  - **Экспорт**: экспорт остатков с изображениями и без изображений
  - **Синхронизация**: включение/отключение синхронизации остатков
  - **Заявки на перемещение**: создание файла заявки для выбранных товаров
  - **Заявки поставщику**: создание файла заявки поставщику
- JavaScript функции для обработки всех действий меню
- Автоматическое закрытие меню при клике вне его области
- Проверка на пустой воркспейс перед выполнением действий

### Изменено
- Расширен функционал воркспейса для массовых операций с выбранными товарами
- Добавлены уведомления о выполнении действий с указанием количества товаров
- Все функции действий показывают соответствующие уведомления пользователю

## [2025-01-19] - Решение проблемы с "липкими" стилями кнопок

### Исправлено
- Проблема с сохранением стилей кнопок после снятия выделения карточки
- Убраны все `!important` правила для кнопок, которые делали стили "липкими"
- Упрощены CSS правила - теперь переопределяется только цвет текста кнопок

### Изменено
- Убраны все `!important` правила для кнопок внутри `.workspace-selected`
- Оставлены только простые правила для цвета кнопок редактирования (синий) и удаления (красный)
- Теперь кнопки корректно возвращаются к своим оригинальным стилям при снятии выделения

## [2025-01-19] - Окончательное исправление стилей кнопок при выделении карточек

### Исправлено
- Проблема с CSS правилом `revert !important` - оно работало неправильно и делало кнопки серыми
- Восстановлены правильные цвета для кнопок редактирования (синий) и удаления (красный)
- Добавлены специфичные CSS правила для каждого типа кнопок с правильными цветами

### Изменено
- Заменено неправильное правило `revert !important` на конкретные цвета для кнопок
- Восстановлены специфичные правила для кнопок редактирования, удаления, управления складом, Allegro и перемещения
- Теперь кнопки сохраняют свои оригинальные цвета при выделении карточки и корректно возвращаются к ним при снятии выделения

## [2025-01-19] - Финальное исправление стилей кнопок при выделении карточек

### Исправлено
- Проблема с сохранением стилей кнопок после снятия выделения карточки
- Убраны избыточные CSS правила с `!important` для кнопок
- Упрощено CSS правило для кнопок внутри выделенных карточек

### Изменено
- Заменено сложное CSS правило с множественными селекторами на простое правило с `revert !important`
- Теперь кнопки корректно возвращаются к своим оригинальным стилям при снятии выделения
- Убраны специфичные правила для каждого типа кнопок

## [2025-01-19] - Исправление визуального выделения карточек в воркспейсе

### Исправлено
- Проблема с сохранением визуального выделения карточек после удаления товаров из воркспейса
- Добавлены вызовы `highlightSelectedProducts()` в функции `addProduct()`, `removeProduct()` и `clearWorkspace()`
- Теперь визуальное выделение корректно обновляется при любых изменениях в воркспейсе

### Изменено
- Функция `addProduct()` теперь вызывает `highlightSelectedProducts()` для обновления выделения
- Функция `removeProduct()` теперь вызывает `highlightSelectedProducts()` для снятия выделения
- Функция `clearWorkspace()` теперь вызывает `highlightSelectedProducts()` для снятия всех выделений
- Обеспечена синхронизация между состоянием воркспейса и визуальным выделением карточек

## [2025-01-19] - Исправление стилей кнопок при выделении карточек

### Исправлено
- Проблема с изменением стилей кнопок редактирования и удаления при выделении карточек товаров
- Добавлены специфичные CSS правила для сохранения оригинальных стилей кнопок внутри выделенных карточек
- Исправлены стили для всех типов кнопок: редактирование, удаление, управление складом, управление Allegro, перемещение

### Изменено
- Добавлены CSS правила с `!important` для кнопок внутри `.workspace-selected` карточек
- Сохранены оригинальные цвета и стили для всех кнопок независимо от состояния выделения карточки

## [2025-01-19] - Полное удаление функциональности экспорта из каталога

### Удалено
- Кнопка "Экспорт" из заголовка воркспейса
- Кнопка "Экспортировать" из основного блока каталога
- Функция `exportWorkspace()` из JavaScript кода воркспейса
- Функция `exportSelectedProducts()` из JavaScript кода каталога
- Функция `updateExportButton()` из JavaScript кода каталога
- Глобальная функция `exportWorkspace()`
- Все ссылки на функции экспорта в коде каталога и воркспейса
- Обработчики событий для кнопок экспорта

### Изменено
- Упрощен интерфейс воркспейса - теперь доступны только кнопки "Очистить" и "Закрыть"
- Упрощен интерфейс каталога - теперь доступна только кнопка "Добавить товар"
- Полностью удалена функциональность экспорта товаров из интерфейса каталога

## [2025-01-19] - Модернизация карточки товара для нового каталога

### Добавлено
- Отдельный компонент карточки товара для нового каталога (`catalog_new_blocks/product_card.html`)
- API endpoint `/catalog_new/api/products/{sku}/card` для получения карточек товаров
- Полная функциональность карточек товаров в воркспейсе через серверный рендеринг
- **НОВОЕ**: Клик по карточке товара автоматически открывает воркспейс и добавляет товар
- **НОВОЕ**: Улучшенные стили выделения карточек с анимацией hover
- **НОВОЕ**: Функция `addToWorkspaceFromCard()` для добавления товаров в воркспейс

### Изменено
- Роуты каталога теперь используют новый компонент карточки товара
- Воркспейс использует серверный рендеринг карточек через API вместо генерации HTML в JavaScript
- Обновлены все ссылки на компонент карточки в роутерах каталога
- **НОВОЕ**: Убраны чекбоксы из карточек товаров - теперь выбор происходит кликом
- **НОВОЕ**: Кнопки "Выбрать все" и "Отменить выбор" работают с воркспейсом
- **НОВОЕ**: Экспорт работает с товарами из воркспейса вместо чекбоксов
- **НОВОЕ**: Улучшена визуальная обратная связь при взаимодействии с карточками

### Исправлено
- Проблема с использованием настоящих компонентов product_card в воркспейсе
- Теперь воркспейс использует те же компоненты, что и основной каталог
- Ошибка сериализации Decimal в JSON - добавлено преобразование в float
- **НОВОЕ**: Ошибка селектора `span:contains("EAN:")` в функции `extractProductData`
- **НОВОЕ**: Улучшена функция извлечения данных товара из карточек без чекбоксов
- **НОВОЕ**: Добавлены версии скриптов для принудительного обновления кэша браузера
- **НОВОЕ**: Исправлена функциональность drag & drop для карточек товаров
- **НОВОЕ**: Добавлена подсказка при захвате карточки о возможности перетащить в воркспейс
- **НОВОЕ**: Улучшены визуальные эффекты при drag & drop (курсор grab/grabbing, подсветка области воркспейса)
- **НОВОЕ**: Добавлена невидимая drop-зона для воркспейса когда он закрыт
- **НОВОЕ**: Реализована возможность перетаскивания товаров в воркспейс даже когда он закрыт
- **НОВОЕ**: Улучшена визуальная индикация drop-зоны с градиентной подсветкой
- **ИСПРАВЛЕНО**: Проблема с передачей данных товара в drag & drop (undefined в уведомлениях)
- **ИСПРАВЛЕНО**: Добавлены data-атрибуты в старый компонент product_card.html
- **ИСПРАВЛЕНО**: Обновлен основной каталог для использования нового компонента карточки
- **НОВОЕ**: Добавлена отладочная информация для диагностики проблем с данными товаров
- **ИСПРАВЛЕНО**: Убрано автоматическое добавление товара в воркспейс при клике на карточку
- **ИСПРАВЛЕНО**: Обновлены функции selectAllProducts и deselectAllProducts - теперь только визуальное выделение
- **ИСПРАВЛЕНО**: Применение фильтров теперь корректно обновляет список товаров и переинициализирует drag & drop
- **ИЗМЕНЕНО**: Товары в воркспейс добавляются только через drag & drop, клик по карточке больше не добавляет товар
- **ИСПРАВЛЕНО**: Применение фильтров при открытом воркспейсе теперь обновляет только правую панель каталога
- **ИСПРАВЛЕНО**: Функции selectAllProducts и deselectAllProducts теперь работают с правильным контейнером в зависимости от состояния воркспейса
- **ИСПРАВЛЕНО**: Пагинация и переходы между страницами корректно работают при открытом воркспейсе
- **ИСПРАВЛЕНО**: При применении фильтров при открытом воркспейсе сохраняется шапка каталога с кнопками
- **ИСПРАВЛЕНО**: Обновляется только контейнер с товарами, а не вся правая панель каталога
- **ИСПРАВЛЕНО**: Кнопки фильтров и управления остаются доступными при применении фильтров
- **НОВОЕ**: Добавлен обратный drag & drop - товары можно перетаскивать из воркспейса в каталог
- **НОВОЕ**: При перетаскивании товара из воркспейса в каталог товар удаляется из воркспейса
- **НОВОЕ**: Добавлена специальная подсказка при перетаскивании из воркспейса с иконкой корзины
- **НОВОЕ**: Карточки товаров в воркспейсе теперь перетаскиваемые (draggable="true")
- **ОТЛАДКА**: Добавлена подробная отладочная информация для диагностики проблем с обратным drag & drop
- **ОТЛАДКА**: Логирование количества найденных карточек в воркспейсе и их структуры
- **ОТЛАДКА**: Отслеживание флага fromWorkspace и данных передачи при drop операциях
- **ИСПРАВЛЕНО**: Добавлено делегирование событий для drag & drop из воркспейса
- **ИСПРАВЛЕНО**: Разделены обработчики событий на прямые и делегированные для лучшей совместимости
- **ОТЛАДКА**: Добавлено логирование делегированных событий для диагностики проблем
- **ИСПРАВЛЕНО**: Добавлен обработчик drop для области каталога при открытом воркспейсе
- **ИСПРАВЛЕНО**: Теперь товары правильно удаляются из воркспейса при перетаскивании в каталог
- **УДАЛЕНО**: Кнопки "Выбрать все" и "Отменить выбор" из интерфейса каталога
- **УДАЛЕНО**: Выделение карточек товаров (класс "selected" и связанная логика)
- **УДАЛЕНО**: Обработчики чекбоксов и функция handleCheckboxChange
- **УДАЛЕНО**: CSS стили для выделенных карточек товаров
- **ОТЛАДКА**: Добавлена подробная отладка для диагностики проблем с удалением товаров из воркспейса
- **ОТЛАДКА**: Логирование вызовов removeProduct и проверка существования товаров в воркспейсе
- **ИСПРАВЛЕНО**: Добавлен stopPropagation() в обработчики drop для предотвращения конфликта событий
- **ИСПРАВЛЕНО**: Теперь срабатывает только один обработчик drop вместо двух одновременно
- **ИСПРАВЛЕНО**: Общий обработчик dragstart теперь не перезаписывает данные товаров из воркспейса
- **ИСПРАВЛЕНО**: Добавлена проверка isFromWorkspace для предотвращения конфликта обработчиков
- **ИСПРАВЛЕНО**: Добавлены обработчики dragover и dragleave для области каталога
- **ИСПРАВЛЕНО**: Теперь событие drop может срабатывать на области каталога при перетаскивании из воркспейса
- **НОВОЕ**: Добавлена отдельная drop-зона для области каталога (#catalogDropZone)
- **НОВОЕ**: Drop-зона каталога активируется только когда воркспейс открыт
- **НОВОЕ**: Визуальная подсказка "Отпустите здесь, чтобы удалить из воркспейса" для drop-зоны каталога
- **НОВОЕ**: Красная подсветка и граница для drop-зоны каталога при перетаскивании

---

## [2025-01-19] - Исправление автоматического применения фильтров в каталоге

### Исправлено
- Убрано автоматическое применение фильтров при изменении значений в полях
- Фильтры теперь применяются только при нажатии кнопки "Применить" или клавиши Enter
- Убран таймаут в 300ms для автоматического применения фильтров
- Добавлены обработчики событий для кнопки "Применить фильтры" и клавиши Enter
- **КРИТИЧЕСКОЕ**: Убраны все обработчики `oninput` и `onchange` из HTML шаблона `filter_slider.html`
- **Улучшение UX**: Убрано автоматическое закрытие слайдера фильтров при клике вне его области

### Изменено
- Функция `handleFilterChange()` больше не использует `setTimeout` для автоматического применения
- Добавлены обработчики для кнопки с ID `applyFilters`
- Добавлены обработчики для клавиши Enter в полях фильтров: `searchInput`, `stockFilter`, `minStockFilter`
- Фильтры применяются только при явном действии пользователя

### Технические детали
- Убран `clearTimeout(searchTimeout)` и `setTimeout` из функции `handleFilterChange()`
- Добавлены обработчики событий в секции инициализации `DOMContentLoaded`
- Обработчик клавиши Enter предотвращает стандартное поведение и вызывает `applyFilters()`
- **Убраны все HTML обработчики**: `oninput="handleFilterChange()"` и `onchange="handleFilterChange()"` из всех полей фильтров
- **Убран обработчик клика вне области**: слайдер больше не закрывается автоматически при клике вне его
- Сохранена вся существующая функциональность фильтрации

### Улучшено
- Пользовательский опыт: фильтры не применяются случайно при вводе
- Контроль пользователя: явное применение фильтров по желанию
- Производительность: нет лишних запросов при вводе текста
- Предсказуемость: фильтры работают только когда пользователь этого хочет
- **Удобство работы**: слайдер фильтров остается открытым при клике вне его области

---

## [2024-12-19] - Рефакторинг sync_tasks.py для использования микросервиса Allegro

### Добавлено
- Вспомогательные функции для работы с микросервисом Allegro:
  - `get_microservice_jwt_token()` - создание JWT токена для аутентификации
  - `get_active_tokens_from_microservice()` - получение активных токенов из микросервиса
  - `get_offers_from_microservice()` - получение офферов через микросервис
  - `update_offer_stock_via_microservice()` - обновление остатков через микросервис
  - `update_offer_price_via_microservice()` - обновление цен через микросервис

### Изменено
- Полностью переписаны все Celery задачи синхронизации для использования микросервиса Allegro:
  - `sync_allegro_stock_all_accounts()` - массовая синхронизация остатков по всем аккаунтам
  - `sync_allegro_stock_single_product()` - синхронизация остатков одного товара по всем аккаунтам
  - `sync_allegro_stock_single_account()` - синхронизация остатков для одного аккаунта
  - `sync_allegro_stock_single_product_account()` - синхронизация остатков конкретного товара с конкретным аккаунтом
  - `sync_allegro_price_single_product_account()` - синхронизация цен конкретного товара с конкретным аккаунтом
  - `sync_allegro_offers_batch()` - обработка батча SKU для одного аккаунта
  - `update_single_allegro_offer()` - обновление оффера через микросервис
  - `sync_all_offers_for_account()` - синхронизация цен для всех товаров конкретного аккаунта

### Улучшено
- Архитектура системы: все задачи синхронизации теперь работают через микросервис Allegro
- Масштабируемость: микросервис обеспечивает лучшую изоляцию и управление токенами
- Надежность: централизованное управление токенами через микросервис
- Производительность: оптимизированные запросы к микросервису с батчингом

---

## [2024-12-19] - Рефакторинг роутеров products.py для использования микросервиса Allegro

### Изменено
- Обновлены все роутеры в `app/api/v1/routers/products.py` для работы через микросервис Allegro:
  - `sync_all_stocks_for_account()` - синхронизация остатков всех товаров аккаунта
  - `sync_all_offers_for_account()` - синхронизация цен всех товаров аккаунта
  - `update_sync_settings()` - обновление настроек синхронизации товара
  - `sync_price_single_product_account()` - синхронизация цены конкретного товара
  - `manage_product()` - страница управления товаром
  - `get_product_offers()` - получение оферт товара

### Исправлено
- Обращение к атрибутам объектов токенов вместо словарей
- Создание клиентов микросервиса с правильными параметрами аутентификации
- Обработка ошибок при получении токенов из микросервиса

### Улучшено
- Консистентность архитектуры: все компоненты системы теперь работают через микросервис Allegro
- Единообразие: все роутеры используют одинаковый подход для получения токенов
- Надежность: централизованное управление токенами через микросервис

---

## [2024-12-19] - Добавление кнопки синхронизации остатков всех товаров аккаунта

### Добавлено
- Кнопка "Синхронизировать остатки всех товаров" в блок синхронизации остатков для каждого аккаунта Allegro
- Новый API эндпоинт `/api/products/sync-settings/{account_name}/sync-stocks` для синхронизации остатков
- JavaScript функция `syncAllAccountStocks()` для обработки синхронизации остатков всех товаров аккаунта
- Визуальное разделение между различными кнопками синхронизации

### Изменено
- Улучшен интерфейс управления товаром с более удобным доступом к синхронизации остатков
- Добавлена отдельная кнопка для синхронизации остатков (отдельно от синхронизации цен)

### Улучшено
- Пользовательский опыт: теперь можно легко синхронизировать остатки всех товаров конкретного аккаунта
- Логическое разделение функций: синхронизация остатков отделена от синхронизации цен

---

## [2024-12-19] - Рефакторинг нового каталога на модульные блоки

### Добавлено
- Директория `app/templates/catalog_new_blocks/` для модульной структуры каталога
- Блок `header.html` - заголовок с информацией о фильтре по операции
- Блок `main_content.html` - основной контент с заголовком, кнопками и списком товаров
- Блок `pagination.html` - пагинация каталога
- Блок `empty_state.html` - состояние пустого результата
- Блок `filter_slider.html` - слайдер фильтров
- Блок `filter_toggle.html` - кнопка переключения фильтров
- Блок `scripts.html` - JavaScript код для функциональности каталога

### Изменено
- Основной шаблон `catalog_new.html` теперь собирает страницу из отдельных блоков
- Улучшена читаемость и поддерживаемость кода каталога
- Модульная структура позволяет легко изменять отдельные части каталога
- Подключен новый роутер каталога в `app/api/v1/api.py` с префиксом `/catalog`

### Улучшено
- Архитектура: разделение логики на тематические блоки
- Поддерживаемость: каждый блок отвечает за свою функциональность
- Читаемость: основной шаблон стал намного проще и понятнее
- Гибкость: возможность переиспользования блоков в других частях приложения

### Доступные маршруты нового каталога:
- **Главная страница**: `/catalog` - новый каталог товаров
- **API товаров**: `/catalog/api/products` - получение списка товаров с фильтрацией
- **API брендов**: `/catalog/api/brands` - получение списка брендов
- **Старый каталог**: остается доступным по старым маршрутам

## [2024-12-19] - Создание нового каталога товаров с отдельными маршрутами и слайдером фильтров

### Добавлено
- Новый роутер `app/api/v1/routers/catalog.py` для каталога товаров
- Новый шаблон `app/templates/catalog_new.html` с современным дизайном
- Слайдер фильтров, фиксированный снизу страницы с плавной анимацией
- Отдельные API эндпоинты для каталога: `/catalog/api/products` и `/catalog/api/brands`
- Кнопка переключения фильтров в правом нижнем углу

### Изменено
- Обновлена основная маршрутизация в `app/api/v1/api.py` для включения нового роутера каталога
- Новый каталог доступен по маршруту `/catalog` (старый остается доступным)
- Фильтры вынесены в отдельный слайдер снизу страницы для лучшего UX

### Технические детали
- Новый каталог использует отдельные эндпоинты для AJAX запросов
- Слайдер фильтров поддерживает все существующие фильтры: поиск, остатки, бренды, сортировка
- Адаптивный дизайн с поддержкой мобильных устройств
- Плавные анимации открытия/закрытия слайдера фильтров
- Сохранение состояния фильтров при навигации

### Улучшено
- Пользовательский опыт: фильтры не занимают место в основном контенте
- Производительность: отдельные API эндпоинты для каталога
- Архитектура: разделение логики каталога и продуктов
- Современность: новый дизайн с улучшенной навигацией

## [2024-12-28] - Исправление макета страницы управления продуктами: улучшение разделения блоков и структуры офертов

### Исправлено
- Изменена сетка блоков аккаунтов с `lg:grid-cols-2` на `xl:grid-cols-3` для лучшего разделения по ширине
- Убран вложенный grid для синхронизации цен и мультипликатора, теперь все блоки на одном уровне
- Перенесены оферты из отдельного блока внизу страницы в каждый конкретный аккаунт
- Добавлен разделитель между настройками аккаунта и его офертами

### Изменено
- Структура отображения: теперь каждый аккаунт содержит свои настройки и оферты в одном блоке
- Размеры блоков: синхронизация остатков, синхронизация цен и мультипликатор теперь равномерно распределены
- Блок офертов: интегрирован в каждый аккаунт с уменьшенными отступами и размерами
- Адаптивность: улучшена работа на больших экранах (xl и выше)

### Улучшено
- Макет: блоки аккаунтов теперь правильно разделяются по ширине без "уплывания" крайнего блока
- Логичность: оферты отображаются для каждого конкретного аккаунта, а не все вместе
- Визуальная организация: четкое разделение между настройками и офертами каждого аккаунта
- Пространство: более эффективное использование ширины экрана на больших дисплеях

## [2024-12-28] - Добавление логики отображения кнопки "Создать товар" только для несуществующих товаров

### Добавлено
- Поле `product_exists` в схему `StockValidationResult` для отслеживания существования товара в базе данных
- Логика проверки существования товара в таблице `Product` перед проверкой остатков на складах
- Условное отображение кнопки "Создать товар" только когда товар не существует в базе данных

### Изменено
- Обновлен `StockValidationService.validate_stock_deduction()` для проверки существования товара в таблице `Product`
- Обновлен `StandardizedLogger.log_validation_failure()` для передачи поля `product_exists` в детали валидации
- Обновлен шаблон `validation_errors.html` для условного отображения кнопки создания товара

### Технические детали
- Кнопка "Создать товар" теперь появляется только при `product_exists = False`
- При `product_exists = True` (товар существует, но недостаточно остатков) кнопка не отображается
- Улучшена логика валидации: сначала проверяется существование товара, затем остатки на складах
- Добавлено поле `product_exists` во все результаты валидации для корректного отображения UI

### Улучшено
- Пользовательский опыт: кнопка "Создать товар" появляется только когда это действительно необходимо
- Логичность интерфейса: устранено дублирование кнопок для товаров, которые уже существуют
- Точность валидации: четкое различие между "товар не существует" и "товар есть, но недостаточно остатков"

## [2024-12-28] - Обновление логики подсчета незавершенных операций: отслеживание всех операций кроме завершенных и отмененных

### Изменено
- Логика подсчета незавершенных операций теперь включает все статусы кроме CANCELLED и COMPLETED
- Обновлен метод `get_sync_statistics` в `StockSynchronizationService` для использования `status.not_in()`
- Обновлена страница мониторинга для отображения корректного количества незавершенных операций
- Изменен заголовок страницы на "Незавершенные операции" для ясности

### Технические детали
- Статус "Незавершенные" теперь включает операции со статусами:
  - PENDING (В ожидании)
  - PROCESSING (В обработке) 
  - STOCK_DEDUCTED (Остатки списаны)
  - FAILED (Провалено)
- Обновлены SQL запросы для использования `status.not_in([CANCELLED, COMPLETED])`
- Обновлены заголовки и описания в интерфейсе для отражения новой логики
- Добавлено пояснение "(все кроме завершенных и отмененных)"

### Улучшено
- Точность статистики: корректный подсчет всех операций, требующих внимания
- Логичность: более понятная группировка операций по принципу "завершено/не завершено"
- Прозрачность: пользователь видит реальное количество активных операций в системе
- Информативность: четкое понимание, что отслеживается

## [2024-12-28] - Оптимизация загрузки статуса списания: использование данных из fullOrder вместо API запроса

### Изменено
- Функция `loadStockStatus()` теперь использует данные из `fullOrder.technical_flags.is_stock_updated` вместо API запроса
- Убран асинхронный API запрос к `/api/stock-status/${operationOrderId}/${operationTokenId}`
- Функция `loadStockStatus()` больше не является асинхронной

### Технические детали
- Статус списания теперь загружается из уже доступных данных заказа
- Добавлен fallback на статус "Не списано" если данные о статусе списания отсутствуют
- Устранено дублирование логики проверки статуса списания
- Улучшена производительность: нет лишних HTTP запросов

### Улучшено
- Производительность: устранен ненужный API запрос для получения статуса списания
- Консистентность данных: статус списания берется из того же источника, что и остальные данные заказа
- Стабильность: нет зависимости от внешнего API для отображения статуса списания

## [2025-01-13] - Система управления складами
### Добавлено
- Универсальный менеджер складов `scripts/warehouse_manager.py` с возможностью переименования, мониторинга и анализа
- Специализированный скрипт `scripts/rename_warehouse.py` для переименования складов
- Команды в Makefile для управления складами: `warehouse-list`, `warehouse-info`, `warehouse-rename`, `warehouse-rename-dry-run`
- Автоматическое обновление перечисления Warehouses в коде при переименовании
- Поддержка всех связанных таблиц: stock, sale, transfer, operation, pending_stock_operation

### Изменено
- Обновлена документация проекта с описанием системы управления складами
- Добавлены примеры использования в Makefile

### Исправлено
- Нет исправлений в данном обновлении

---

## [2024-12-28] - Исправление логики перемещения товара: перерисовка карточки вместо перезагрузки страницы
### Исправлено
- Изменена логика функции `updateProductCard` для корректной работы с панелью быстрого редактирования
- Убрана попытка найти и заменить отдельную карточку товара по селектору `[data-product-id="${sku}"]`
- Добавлена функция `reloadQuickEditContent` для перезагрузки содержимого панели быстрого редактирования
- Функция `reloadQuickEditContent` сделана доступной глобально через `window` объект

### Технические детали
- После успешного перемещения товара теперь перезагружается содержимое панели быстрого редактирования
- Добавлена проверка, что панель быстрого редактирования открыта перед попыткой обновления
- Функция `updateProductCard` теперь использует `reloadQuickEditContent` вместо попытки найти отдельную карточку
- Сохранена логика fallback на перезагрузку страницы в случае ошибок или отсутствия данных операции

### Улучшено
- Пользовательский опыт: при перемещении товара страница не перезагружается, а карточка перерисовывается
- Производительность: устранена ненужная перезагрузка страницы при успешных операциях
- Стабильность: корректная обработка случаев, когда панель быстрого редактирования не открыта
- Консистентность: единообразное поведение для операций перемещения и редактирования цены

## [2024-12-28] - Добавление автоматической перезагрузки блока с товарами после операций списания и пополнения
### Добавлено
- Автоматическая перезагрузка блока с товарами через 3 секунды после успешного списания товара
- Автоматическая перезагрузка блока с товарами через 3 секунды после успешного пополнения товара
- Логирование автоматических перезагрузок для отслеживания процесса

### Технические детали
- Функция `removeFromStock` теперь автоматически вызывает `reloadQuickEditContent` через 3 секунды
- Функция `addToStock` теперь автоматически вызывает `reloadQuickEditContent` через 3 секунды
- Добавлена обработка ошибок для автоматических перезагрузок
- Проверка наличия SKU товаров операции перед попыткой перезагрузки

### Улучшено
- Пользовательский опыт: данные о товарах автоматически обновляются после операций со складом
- Актуальность данных: пользователь видит актуальную информацию о товарах без ручного обновления
- Консистентность: единообразное поведение для операций списания и пополнения

## [2024-12-28] - Исправление JavaScript ошибки: функция addToStock не определена
### Исправлено
- Добавлена недостающая функция `addToStock` в файл `operation_details_blocks/scripts.html`
- Добавлена функция `removeFromStock` для списания товаров со склада
- Добавлена функция `updateTotalStock` для обновления общего количества товара
- Добавлена функция `showNotification` для отображения уведомлений пользователю
- Добавлена функция `initializeWarehouseQuantities` для инициализации количества товаров на складах
- Добавлена функция `openTransferModal` для открытия модального окна перемещения товаров
- Добавлена функция `closeTransferModal` для закрытия модального окна перемещения
- Добавлена функция `editPrice` для редактирования цены товара
- Добавлена функция `closePriceEditModal` для закрытия модального окна редактирования цены
- Добавлена функция `deleteProduct` для удаления товара
- Добавлена функция `editProduct` для редактирования товара
- Добавлена функция `updateProductCard` для обновления карточки товара
- Все функции сделаны доступными глобально через `window` объект

### Технические детали
- Функции загружаются динамически через AJAX-запрос к `/products/quick_edit`
- Исправлена ошибка "ReferenceError: addToStock is not defined" при работе с окном товаров
- Исправлены ошибки "ReferenceError: openTransferModal is not defined", "editPrice is not defined", "deleteProduct is not defined", "editProduct is not defined"
- Решена проблема отображения "NaN шт." вместо корректного количества товара
- Добавлена автоматическая инициализация `data-currentQuantity` атрибутов на кнопках
- Восстановлена функциональность кнопок пополнения и списания товаров на складе
- Восстановлена функциональность модальных окон для перемещения товаров и редактирования цен
- Добавлена валидация ввода и обработка ошибок для всех операций со складом

### Улучшено
- Пользовательский опыт: кнопки товаров теперь работают корректно
- Стабильность интерфейса: устранены все JavaScript ошибки при работе с панелью товаров
- Функциональность управления складом: восстановлены все операции с товарами
- Функциональность управления товарами: восстановлены операции редактирования, удаления и перемещения
- Отображение количества: корректное отображение количества товара вместо "NaN шт."
- Автоматическая инициализация: количество товаров автоматически устанавливается при загрузке панели
- Модальные окна: восстановлена работа всех модальных окон для управления товарами
- Динамическое обновление: карточки товаров обновляются без перезагрузки страницы при перемещении и редактировании цены
- Обработчики форм: добавлены обработчики для форм перемещения и редактирования цены с валидацией

## [2024-12-28] - Добавление цветных фильтров для статуса заказа
### Добавлено
- Красивые цветные теги для статуса заказа аналогично статусу операции
- Цветовая схема: Отправлен (зеленый), Готов к отправке (синий), Заполнен (желтый), Отмены (красные)
- Функции `order_status_label` и `order_status_color` в `filters.py` для локализации и цветового оформления
- Обновление JavaScript функций для корректного отображения HTML тегов

### Изменено
- HTML шаблон `operation_details_blocks/header.html`: статус заказа теперь отображается в цветных тегах
- JavaScript функция `getOrderStatus` в `operation_details_blocks/scripts.html`: возвращает HTML с цветными тегами
- JavaScript функция `getOrderStatus` в `allegro_orders.html`: обновлена логика и цветовая схема
- Замена `textContent` на `innerHTML` для корректного отображения HTML тегов

### Улучшено
- Визуальное восприятие статусов заказа: каждый статус имеет свой цвет для быстрой идентификации
- Единообразие интерфейса: статус заказа теперь оформлен аналогично статусу операции
- Пользовательский опыт: цветовое кодирование упрощает понимание состояния заказа

## [2024-12-28] - Изменение primary key моделей Sale и Transfer с int на UUID
### Изменено
- Модель `Sale` теперь использует UUID вместо int для поля `id`
- Модель `Transfer` теперь использует UUID вместо int для поля `id`
- Добавлен импорт модуля `uuid` и типа `UUID` из PostgreSQL
- Настроен `default_factory` для автоматической генерации UUID при создании новых записей

### Технические детали
- Primary key изменен с `Optional[int]` на `Optional[str]`
- SQLModel автоматически создает колонку с типом `String` для UUID primary key
- `default_factory=lambda: str(uuid.uuid4())` автоматически генерирует UUID для новых записей
- Существующие записи потребуют миграции для преобразования int в UUID

### Исправлено
- Убрана ошибка `RuntimeError: Passing primary_key is not supported when also passing a sa_column`
- Упрощена конфигурация полей primary key для совместимости с SQLModel

### Зависимости
- Требуется создание миграции для преобразования существующих данных
- Все внешние ключи, ссылающиеся на эти таблицы, должны быть обновлены

## [2024-12-28] - Частичное восстановление функциональности: возврат кнопки "Пометить списанным"
### Изменено
- Раскомментирована кнопка "Пометить списанным" для заказов, которые не списаны
- Кнопка "Списать" остается закомментированной
- Обновлена логика формирования кнопок действий

### Технические детали
- Кнопка "Пометить списанным" снова активна и отображается для несписанных заказов
- Кнопка "Списать" по-прежнему отключена
- Функциональность пометки заказа как списанного восстановлена

## [2024-12-28] - Временное отключение кнопок "Списать" и "Пометить списанным" в интерфейсе заказов Allegro
### Изменено
- Закомментированы блоки кода, отвечающие за отображение кнопок "Списать" и "Пометить списанным"
- Закомментирована неиспользуемая переменная `canShowWriteOff`
- Кнопки действий больше не отображаются в интерфейсе заказов

### Технические детали
- Кнопки "Списать" и "Пометить списанным" временно отключены
- Функциональность сохранена в коде, но скрыта от пользователя
- Возможность быстрого восстановления функциональности при необходимости

## [2024-12-28] - Ограничение отображения кнопки "Детали операции" только для заказов со статусом READY_FOR_PROCESSING
### Изменено
- Кнопка "Детали операции" теперь отображается только для заказов со статусом "READY_FOR_PROCESSING"
- Улучшена логика отображения элементов управления в интерфейсе заказов Allegro

### Улучшено
- Пользовательский интерфейс: кнопка "Детали операции" показывается только когда это логично
- Предотвращение попыток просмотра деталей операции для заказов, которые еще не готовы к обработке

## [2024-12-28] - Добавление точечного перехода к заказу из деталей операции
### Добавлено
- API endpoint `/api/operations/by-order/{order_id}` теперь возвращает `token_id` и `account_name` для операций синхронизации
- Кнопка "Перейти к заказу" на странице детали операции для прямого перехода к конкретному заказу
- Автоматический поиск по ID заказа при переходе со страницы деталей операции
- Поддержка параметра `search` в URL для автоматического поиска заказа при загрузке страницы

### Изменено
- Логика API endpoint: сначала ищет в `PendingStockOperation`, затем в обычных операциях
- Кнопка "Перейти к заказам" заменена на "Перейти к заказу" при наличии всех необходимых данных
- Страница заказов Allegro теперь автоматически выполняет поиск при наличии параметра `search` в URL

### Улучшено
- Навигация между страницей деталей операции и конкретным заказом
- Пользовательский опыт: прямой переход к нужному заказу без необходимости поиска
- Интеграция между системой операций и системой заказов Allegro

## [2024-12-28] - Добавление кнопки "Перейти к заказам" в детали операции
### Добавлено
- Кнопка "Перейти к заказам" на странице деталей операции для быстрого перехода к списку заказов Allegro
- Кнопка отображается только для операций, связанных с заказами (при наличии `order_id`)
- Визуальное оформление кнопки в синем цвете с иконкой 📋 для выделения

### Улучшено
- Навигация между страницей деталей операции и списком заказов
- Пользовательский опыт: возможность быстро вернуться к заказам для дальнейшего анализа

## [2024-12-28] - Изменение поведения клика на заказ в Allegro - возврат логики отображения деталей с добавлением кнопки "Детали операции"
### Изменено
- Возвращена логика отображения деталей заказа при клике на заказ в списке заказов Allegro
- Восстановлена функция разворачивания деталей заказа (`toggleOrderDetails`)
- Восстановлены блоки с детальной информацией о покупателе, доставке и товарах

### Добавлено
- Новый API endpoint `/api/operations/by-order/{order_id}` для получения операции по ID заказа
- Новый маршрут `/stock-sync/monitor/operation-by-order/{order_id}` для отображения страницы деталей операции по order_id
- Кнопка "Детали операции" в каждом заказе для перехода на страницу деталей операции
- Функция `goToOperationDetails(orderId)` для перехода на страницу деталей операции
- CSS стили для tooltip при наведении на кнопки копирования

### Улучшено
- Пользовательский опыт: теперь можно как просматривать детали заказа, так и переходить к деталям операции
- Интеграция между системой заказов Allegro и системой мониторинга операций
- Единообразный интерфейс для просмотра деталей операций
- Визуальное оформление кнопки "Детали операции" в фиолетовом цвете для выделения

## [2024-12-28] - Замена Token ID на название аккаунта в деталях операции
### Изменено
- В шаблоне `operation_details.html` заменено отображение "Token ID" на "Аккаунт"
- Теперь вместо технического ID токена отображается читаемое название аккаунта Allegro
- Добавлена обработка случая, когда название аккаунта не указано (отображается "Не указан")

## [2024-12-28] - Исправление формирования URL для operation_id и полная локализация логов
### Исправлено
- Исправлена корявая ссылка в Telegram уведомлениях - теперь правильно формируется URL для operation_id
- Исправлена функция `_get_operation_url` в сервисе уведомлений для корректного формирования параметров URL
- Убрана неправильная формация operation_key в уведомлениях о провале валидации
- Решена проблема с постоянной регистрацией отключенной задачи `validate-pending-operations` в Celery Beat

### Изменено  
- Все сообщения в `self._log_operation` переведены на русский язык (26 сообщений в файле stock_synchronization_service.py)
- URL для операций теперь формируются как `stock-sync/monitor?operation_id={operation_id}` вместо неправильного пути
- Уведомления о провале валидации теперь ссылаются на мониторинг с фильтрами по order_id и account
- Отключена задача `validate-pending-operations` в `DEFAULT_BEAT_SCHEDULE` для предотвращения автоматической регистрации

## [2024-12-28] - Добавление функциональности товаров операции
### Добавлено
- Секция "Товары операции" на странице деталей операции с отображением всех товаров
- Кнопка "Перейти к товарам" для перехода в каталог с фильтрацией по товарам конкретной операции
- Кнопка "Создать товар" для товаров, которых нет в базе данных
- Предзаполнение полей формы создания товара данными из операции (SKU, название, количество)
- Фильтрация каталога по SKU операции через параметр `operation_skus`
- Уведомление о применении фильтра по операции в каталоге с возможностью сброса

### Улучшено
- Страница деталей операции теперь показывает полную информацию о товарах
- Каталог поддерживает фильтрацию по товарам конкретной операции
- Форма создания товара автоматически заполняется данными из операции
- Добавлена визуальная индикация существования товара в базе данных

### Исправлено
- Ошибка "must be real number, not str" при отображении цены товара в деталях операции
- Корректная работа с данными `line_items` как со словарями, а не объектами
- Добавлена обработка отсутствующих полей цены и валюты
- Исправлено использование правильного SKU: теперь используется `external.id` вместо `offer.id`

### Изменено
- Обновлен шаблон `operation_details.html` с новой секцией товаров
- Модифицирован роутер каталога для поддержки фильтрации по SKU операции
- Обновлен роутер добавления товара для поддержки предзаполнения полей
- Добавлена проверка существования товаров в базе данных в деталях операции

## [2024-12-28] - Локализация и улучшение UX мониторинга складских операций
### Добавлено
- Локализация логов операций синхронизации складов - все действия и сообщения теперь на русском языке
- Функция копирования SKU в интерфейсе деталей операций с уведомлением об успешном копировании
- Улучшенное форматирование Telegram уведомлений для ошибок валидации с группировкой по типам
- Отображение названий товаров, количества и кнопок копирования SKU в Telegram уведомлениях
- Система фильтров для локализации сообщений в Jinja2 шаблонах
- Красивое форматирование подробных данных логов с декодированием Unicode и структурированным отображением

## [2024-12-28] - Добавление проверки состояния заказа в микросервисе перед списанием
### Добавлено
- Новый метод `_check_order_status_in_microservice` для проверки актуального состояния заказа в микросервисе
- Автоматическая проверка флага `is_stock_updated` перед выполнением списания со склада
- Проверка статуса заказа (должен быть READY_FOR_PROCESSING) перед обработкой
- Проверка статуса fulfillment (отмененные заказы помечаются как завершенные)
- Автоматическое завершение операций для уже обработанных заказов

### Изменено
- Метод `process_pending_operations` теперь проверяет состояние заказа в микросервисе перед списанием
- Операции для уже списанных заказов автоматически помечаются как завершенные
- Операции для отмененных заказов автоматически помечаются как завершенные
- Операции для заказов не в статусе READY_FOR_PROCESSING планируются на повторную попытку

### Улучшено
- Предотвращение дублирования списаний для уже обработанных заказов
- Автоматическое завершение операций для заказов, обработанных вручную
- Более надежная обработка заказов с учетом их текущего состояния
- Детальное логирование всех проверок состояния заказа

## [2024-12-28] - Исправление шаблонов и авторизации мониторинга
### Исправлено
- Шаблон `operation_details.html` теперь правильно наследуется от `base.html` и использует Tailwind CSS
- Исправлена проблема с авторизацией в роутах `/stock-sync/monitor` - теперь правильно передаются данные пользователя в шаблоны
- Устранена ошибка "UUID object is not subscriptable" в шаблонах при обработке идентификаторов операций
- Улучшен дизайн страницы деталей операций с использованием современных компонентов Tailwind CSS

### Изменено
- Шаблон `operation_details.html` полностью переписан с использованием Tailwind CSS классов
- Добавлена передача объектов `user` и `current_user` в контекст шаблонов мониторинга
- Улучшена обработка UUID объектов в Jinja2 шаблонах

## [2024-12-28] - Исправление ошибок и улучшение уведомлений
### Исправлено
- Устранена ошибка `'str' is undefined` в методе `is_ready_for_retry` модели `PendingStockOperation`
- Убрано использование несуществующего атрибута `MONITOR_URL` из конфигурации `StockSyncConfig`
- Добавлена обработка ошибок при форматировании логов в функциях `get_operation_details` и `operation_details_page`
- Улучшена стабильность загрузки деталей операций при наличии поврежденных логов

### Добавлено
- Поле `BASE_URL` в основную конфигурацию приложения для генерации ссылок в уведомлениях
- Методы `_get_operation_url` и `_get_monitoring_url` для генерации ссылок на детали операций
- Ссылки в Telegram уведомления на детали операций и страницу мониторинга (при наличии `BASE_URL`)
- Пример настройки `BASE_URL` в `example.env`

### Изменено
- Метод `is_ready_for_retry` теперь принимает `max_retries` как параметр вместо поля модели
- Улучшена проверка на `next_retry_at` в методе `is_ready_for_retry` (добавлена проверка на `None`)
- Сервис уведомлений теперь использует централизованную конфигурацию для базового URL

## [2024-12-28] - Упрощение интерфейса мониторинга синхронизации складов
### Добавлено
- Система фильтрации операций по ID заказа, имени аккаунта и статусу
- Поле `account_name` в модель `PendingStockOperation` для хранения имени аккаунта Allegro
- Новый API endpoint `/api/stock-sync/operations` для получения всех операций с фильтрацией
- Поиск по Enter в полях фильтров для удобства использования
- Локализация статусов операций на русский язык

### Изменено
- Упрощена таблица операций: убраны лишние колонки (ID операции, склад, время создания, позиции)
- Изменена логика попыток: убрано поле `max_retries`, оставлен только `retry_count` (количество выполненных попыток)
- Обновлен метод `increment_retry` в модели для работы без максимального лимита попыток
- Улучшен UI: добавлены фильтры в отдельном блоке с современным дизайном
- Оптимизирована структура данных API ответа

### Исправлено
- Убраны несуществующие поля `sku` и `quantity` из API ответа
- Исправлена работа с полем `line_items` для корректного отображения позиций заказа
- Обновлена логика отображения имени аккаунта (теперь используется поле `account_name`)

## [2024-12-28] - Исправление критических ошибок в API endpoints
### Исправлено
- **КРИТИЧЕСКОЕ**: Ошибка 401 Unauthorized при загрузке данных мониторинга синхронизации складов
- **КРИТИЧЕСКОЕ**: Ошибка "'generator' object has no attribute 'exec'" в API endpoints
- **КРИТИЧЕСКОЕ**: Ошибка "'PendingStockOperation' object has no attribute 'sku'" в API endpoints
- Неправильные пути к API endpoints (исправлено с `/api/stock-sync/` на `/api/v1/stock-sync/`)
- Несовместимость аутентификации: API ожидал Bearer token, веб-интерфейс использовал cookie
- Неправильное использование `get_session()` вместо `get_db()` в зависимостях
- Попытка доступа к несуществующим полям `sku` и `quantity` в модели PendingStockOperation
- Изменены зависимости аутентификации с `get_current_user` на `get_current_user_optional` для совместимости
- Добавлена правильная отправка cookie в JavaScript fetch запросах
- Добавлено автоматическое перенаправление на страницу входа при ошибке аутентификации

### Изменено
- Обновлен JavaScript код в `stock_monitoring.html` для корректной работы с API
- Добавлены заголовки `Accept` и `Content-Type` для API запросов
- Улучшена обработка ошибок аутентификации с пользовательским опытом
- Исправлены все зависимости базы данных в stock_sync роутере
- Добавлена колонка "Позиции" в таблицу мониторинга для отображения информации о line_items
- Обновлена логика отображения данных операций с учетом реальной структуры модели

## [2024-12-28] - Рефакторинг шаблона мониторинга синхронизации складов
### Добавлено
- Интеграция с базовым шаблоном сайта для единообразного дизайна
- Использование Tailwind CSS вместо встроенных стилей для лучшей производительности
- Адаптивный дизайн с использованием Tailwind grid и responsive классов
- Современные hover-эффекты и анимации для карточек статистики

### Изменено
- Полностью переработан HTML-код для использования Jinja2 наследования (`{% extends "base.html" %}`)
- Заменены встроенные CSS стили на Tailwind CSS классы
- Обновлен JavaScript код для работы с Tailwind классами (hidden вместо style.display)
- Улучшена структура таблицы с sticky заголовками и лучшим разделением строк

### Исправлено
- Убраны дублирующиеся HTML теги (html, head, body) для корректного наследования
- Исправлена работа с DOM элементами для совместимости с Tailwind CSS
- Улучшена доступность с помощью семантических Tailwind классов

## [2024-12-28] - Улучшен интерфейс мультипликатора цены
### Добавлено
- Динамическое обновление текста мультипликатора при изменении значения в реальном времени
- Возможность ввода значений меньше 1.0 для создания скидок (минимум 0.1)
- Автоматический расчет и отображение процента наценки/скидки при вводе
- JavaScript функция `updateMultiplierText()` для мгновенного обновления интерфейса

### Изменено
- Изменено минимальное значение мультипликатора с 0.01 на 0.1 для лучшего UX
- Добавлен обработчик `oninput` для мгновенного обновления текста при вводе

### Исправлено
- **КРИТИЧЕСКОЕ**: Исправлена глобальная валидация полей number, которая мешала вводу значений < 1
- Исключены поля мультипликатора и цены из принудительной валидации минимального значения
- Теперь можно свободно вводить значения меньше 1.0 в поле мультипликатора

## [2024-12-28] - Исправлена система подсчета остатков и автообновления
### Исправлено
- **КРИТИЧЕСКОЕ**: Ошибка двойного подсчета общих остатков на складах (показывало удвоенное количество)
- Корректная синхронизация атрибутов data-current-quantity между кнопками и input при изменении остатков
- Функция updateTotalStock() теперь использует только кнопки "Списать" для избежания дублирования
- Обновление всех связанных элементов (input, кнопки списания и пополнения) при изменении остатков

## [2024-12-28] - Добавлено расширенное автообновление оферт
### Добавлено
- Автообновление оферт после обновления мультипликатора цены (с задержкой 3 сек)
- Автообновление оферт после синхронизации цены для аккаунта (с задержкой 3 сек)
- Увеличена задержка автообновления до 3 секунд для корректного отображения изменений

### Исправлено
- Ошибка обработки NULL значений в данных цены от API Allegro
- Корректная обработка случаев когда currentPrice или price равны null

## [2024-12-28] - Обновлен интерфейс блока оферт Allegro
### Добавлено
- Новый дизайн интерфейса оферт: заголовок "Оферта [ID]" + текущая цена справа
- Автоматическое обновление оферт при изменениях цен, остатков и настроек синхронизации
- Кнопка обновления оферт (иконка refresh) для ручного обновления
- Клик по контейнеру оферты разворачивает детальную информацию
- Отображение основных данных: название, остатки (доступно/продано), статус публикации
- Детальная информация об оферте при развороте: статистика, условия продажи, публикация

### Изменено
- Убрана кнопка "Показать/Скрыть оферты", вместо этого оферты отображаются сразу
- Обновлена структура данных API endpoint для корректного извлечения цены из saleInfo.currentPrice
- Упрощен интерфейс: один контейнер для оферты с возможностью разворота по клику
- Улучшена обработка событий для автообновления оферт

### Исправлено
- Ошибка `'AllegroToken' object has no attribute 'expires_at'` в функции получения токенов
- Убрана зависимость от поля `expires_at` в модели AllegroToken
- Исправлена логика проверки токенов в функции `get_token`
- Добавлено детальное логирование для диагностики API endpoint оферт

## [2024-12-28] - Реализован блок оферт Allegro в управлении товарами
### Добавлено
- Новый API endpoint `/api/products/{sku}/offers/{account_name}` для получения оферт товара по аккаунту Allegro
- Разворачивающийся блок оферт в интерфейсе управления товаром (`product_manage.html`)
- Отображение основной цены оферты до разворота блока
- Детальная информация об оферте при развороте: статистика, остатки, публикация, условия продажи
- Автоматическая загрузка сводки оферт при открытии страницы управления товаром
- Кеширование загруженных оферт для оптимизации

### Изменено
- Обновлен шаблон `product_manage.html` с новым блоком для отображения оферт
- Добавлен JavaScript для интерактивного управления блоками оферт
- Улучшена обработка ошибок при загрузке оферт с возможностью повторной попытки

## [2024-12-28] - Добавлено детальное логирование для диагностики API
### Добавлено
- Детальное логирование в sync_allegro_price_single_product_account для отслеживания данных перед отправкой в API
- Форматирование цены с помощью f"{target_price:.2f}" для избежания научной нотации
- Логирование в AllegroApiService (синхронная и асинхронная версии) для отслеживания точных данных запросов
- Логирование endpoint, данных и заголовков для диагностики ошибок 400 Bad Request

### Изменено
- Улучшена обработка ошибок с более детальным логированием в Celery задаче

## [2024-12-28] - Исправление endpoint для обновления цены оффера
### Исправлено
- Метод `update_offer_price` в AllegroApiService теперь использует правильный endpoint `PUT /sale/offers/{offerId}/change-price-commands/{commandId}` согласно официальной документации Allegro API
- Исправлена структура данных для обновления цены оффера: используется `input.buyNowPrice` вместо `sellingMode.price`
- Добавлена генерация уникального UUID для commandId в каждом запросе
- Синхронная и асинхронная версии метода обновлены для корректной работы с новым endpoint

## [2024-12-28] - Обновление цен для всех найденных офферов
### Изменено
- Функция `sync_allegro_price_single_product_account` теперь обновляет цены для всех найденных офферов, а не только для первого
- Добавлена обработка каждого оффера в цикле с индивидуальным сравнением цен
- Возвращаемый результат содержит информацию о всех обновленных и пропущенных офферах
- Улучшено логирование: показывается количество найденных офферов и результат для каждого

### Добавлено
- Массивы `updated_offers` и `skipped_offers` для детального отслеживания результатов
- Счетчик `offers_processed` для контроля общего количества обработанных офферов
- Логирование количества найденных офферов для каждого SKU

## [2025-07-30] - Полная переработка системы логирования
### Добавлено
- Новая система логирования с фильтрацией технических логов
- Цветной вывод в консоль для разных уровней логирования
- Разделение логов по файлам: app.log (все логи), errors.log (только ошибки)
- Специальные логгеры для бизнес-логики (allegro.sync, wix.api, warehouse, etc.)
- Функции для структурированного логирования: log_business_event, log_error_with_context
- Переменные окружения для управления логированием: LOG_LEVEL, ENABLE_SQL_LOGS
- Фильтр BusinessLogicFilter для исключения SQL запросов и технических деталей
- Документация по новой системе логирования (docs/logging_guide.md)

### Изменено
- Полностью переработан app/utils/logging_config.py с новой архитектурой
- Обновлена конфигурация Celery для уменьшения шума в логах
- Изменен уровень логирования SQLAlchemy в alembic.ini на ERROR
- Обновлены импорты в main.py и celery_app.py для использования новой системы
- Добавлены настройки логирования в example.env

### Исправлено
- Устранен избыточный вывод SQL запросов в консоль
- Скрыты технические логи библиотек (urllib3, httpx, asyncio, etc.)
- Минимизированы логи Celery, Redis, PostgreSQL
- Улучшена читаемость логов для отладки бизнес-процессов

## [2024-12-28] - Отключена конвертация валют
### Изменено
- Закомментирован блок конвертации валют в sync_allegro_price_single_product_account
- Система теперь всегда использует PLN для обновления цен в Allegro
- Всегда используется PLN как валюта для обновления цен в Allegro
- Обновлены логи и возврат результата для использования target_currency

## [2024-12-19] - Механизм обновления цен в Allegro с конвертацией валют

### Добавлено
- Новый метод `update_offer_price` в `SyncAllegroApiService` для обновления цены оффера через PATCH запрос
- Новая Celery задача `sync_allegro_price_single_product_account` для синхронизации цены конкретного товара с конкретным аккаунтом
- Новый API endpoint `/{sku}/sync-settings/{account_name}/sync-price` для ручного запуска синхронизации цены
- Интеграция с `NBPClient` для конвертации цен из PLN в валюту оффера на Allegro
- Автоматическое получение валюты оффера через API Allegro без дополнительных запросов
- Применение мультипликатора цены из настроек синхронизации
- Проверка минимальной разности цен (< 0.01) для избежания ненужных обновлений
- Кнопка "Синхронизировать цену" для каждого аккаунта в интерфейсе управления товаром

### Изменено
- Расширен `SyncAllegroApiService` асинхронной версией метода `update_offer_price`
- Обновлена задача `sync_allegro_price_single_product_account` для полноценной работы с ценами и валютами
- Добавлена валидация наличия минимальной цены у товара перед синхронизацией
- Исправлено получение цены товара через `prices_service` вместо несуществующего поля `min_price` в модели Product

### Исправлено
- Исправлен несуществующий метод `get_sync_settings` в `ProductAllegroSyncService`
- Добавлен синхронный метод `get_account_sync_settings_sync` для использования в Celery задачах
- Исправлены импорты celery в роутере products.py
- Исправлено получение цены товара через `prices_service` во всех компонентах системы

### Техническая информация
- Цены конвертируются из PLN (базовая валюта) в валюту оффера на Allegro
- Используется официальный API Национального банка Польши для получения курсов валют
- Система автоматически определяет валюту оффера из существующих данных в Allegro
- Поддержка точности до 0.01 единицы валюты для корректного сравнения цен
- Обновления цен логируются для отслеживания изменений

## [2024-12-19] - Автоматическая синхронизация при переключении настроек Allegro

### Добавлено
- Автоматический запуск синхронизации остатков при включении настройки `stock_sync_enabled`
- Автоматический запуск синхронизации цен при включении настройки `price_sync_enabled`
- Автоматический запуск синхронизации цен при обновлении мультипликатора цены
- Новая Celery задача `sync_allegro_stock_single_product_account` для синхронизации остатков конкретного товара с конкретным аккаунтом
- Новая Celery задача `sync_allegro_price_single_product_account` для синхронизации цен конкретного товара с конкретным аккаунтом

### Изменено
- Endpoint `/{sku}/sync-settings/{account_name}/toggle-stock` теперь автоматически запускает синхронизацию остатков при включении
- Endpoint `/{sku}/sync-settings/{account_name}/toggle-price` теперь автоматически запускает синхронизацию цен при включении
- Endpoint `/{sku}/sync-settings/{account_name}/update-multiplier` теперь автоматически запускает синхронизацию цен при обновлении мультипликатора

### Техническая информация
- Задачи синхронизации запускаются асинхронно и не блокируют HTTP-ответ
- При ошибке запуска задачи ошибка логируется, но не прерывает выполнение основного запроса
- Задачи проверяют актуальность настроек синхронизации перед выполнением
- Используется существующая инфраструктура `sync_allegro_offers_batch` для реальной синхронизации

## [2024-12-19] - Исправление функции перемещения товаров

### Исправлено
- Исправлена ошибка 422 при перемещении товаров на странице управления товаром
- Заменен неправильный endpoint `/api/warehouse/transfer/` на правильный `/api/warehouse/transfer-item/`
- Удалено поле `comment` из запроса перемещения, так как API его не поддерживает
- Убрано поле для комментария из формы перемещения товаров
- Теперь перемещение товаров работает корректно с правильной моделью данных

### Техническая информация
- Endpoint `/api/warehouse/transfer/` предназначен для загрузки файлов с перемещениями
- Endpoint `/api/warehouse/transfer-item/` предназначен для отдельных перемещений товаров
- Модель `TransferItem` содержит поля: `sku`, `from_warehouse`, `to_warehouse`, `quantity`

## [2024-12-19] - Обновление системы синхронизации остатков с Allegro

### Изменено
- Обновлена архитектура задач синхронизации остатков для учета индивидуальных настроек товаров
- Функция `sync_allegro_stock_single_product` теперь проверяет флаг `stock_sync_enabled` для каждого аккаунта
- Функция `sync_allegro_stock_single_account` получает только товары с включенной синхронизацией для конкретного аккаунта
- Функция `sync_allegro_stock_all_accounts` делегирует логику выбора товаров в `sync_allegro_stock_single_account`
- Функция `sync_allegro_offers_batch` фильтрует SKU по настройкам синхронизации перед обработкой
- Добавлены синхронные версии методов в `ProductAllegroSyncService`: `should_sync_product_sync` и `get_products_for_stock_sync_sync`

### Добавлено
- Интеграция с моделью `ProductAllegroSyncSettings` в задачи синхронизации
- Проверка настроек синхронизации для каждой пары товар-аккаунт перед запуском синхронизации
- Детальное логирование для отслеживания, какие товары синхронизируются с какими аккаунтами
- Обратная совместимость: при отсутствии настроек проверяется флаг `is_sync_enabled` в модели `Product`

### Исправлено
- Устранена проблема запуска синхронизации для всех аккаунтов без учета индивидуальных настроек
- Оптимизирован процесс синхронизации: теперь обрабатываются только товары с включенной синхронизацией
- Улучшена производительность за счет фильтрации товаров на уровне задач

## [2024-01-17] - Переход к минимальной структуре данных для цен

### Изменено
- Полностью переведена система на минимальную структуру данных для цен
- В базе данных: только sku (PRIMARY KEY) и min_price (NUMERIC)
- Удалены поля price, currency, updated_at из всех эндпоинтов и шаблонов
- Заменены price_info.price на price_info.min_price во всех маршрутах
- Упрощена PriceDataResponse: убраны @property методы для совместимости
- Обновлены шаблоны product_card.html и product_manage.html для работы с min_price
- Исправлены JavaScript функции для работы с минимальной структурой
- Валюта теперь фиксированная PLN во всех отображениях

### Исправлено
- Устранены ошибки в логах: "'PriceDataResponse' object has no attribute 'updated_at'"
- Исправлена несогласованность между структурой БД и кодом приложения
- Исправлена логика отображения цены: теперь цена 0 отображается корректно, а "Не указана" показывается только при отсутствии записи в БД
- Изменены условия с `{% if product.min_price %}` на `{% if product.min_price is not none %}` в шаблонах
- Исправлена проблема с редактированием цены в каталоге: перенесены модальное окно и JavaScript функции из product_card.html в catalog.html для избежания конфликтов ID

## [2024-12-20] - Интеграция с удаленной БД цен товаров

### Добавлено
- Создан сервис для работы с удаленной БД цен (`app/services/prices_service.py`)
- Добавлены модели для работы с таблицей `prices_data` (PriceData, PriceDataCreate, PriceDataUpdate, PriceDataResponse)
- Создан класс PricesService с полным интерфейсом для работы с ценами
- Добавлены API роуты для управления ценами (`/api/prices/`)
- Интеграция отображения цен в карточках товаров каталога
- Добавлено редактирование цен администраторами прямо в карточке товара
- Отображение цен на странице управления товаром с возможностью редактирования
- Поддержка валют (PLN по умолчанию) для цен товаров
- Массовые операции с ценами (batch update, import)
- Статистика по ценам (количество, средняя/мин/макс цена)
- Адаптация под реальную структуру таблицы: `sku` (PRIMARY KEY), `min_price` (NUMERIC)

### Улучшено
- Карточки товаров теперь показывают актуальные цены из удаленной БД
- Добавлены модальные окна для редактирования цен администраторами
- Автоматическое обогащение данных о товарах ценами при загрузке каталога
- Graceful fallback при недоступности БД цен

### Техническое описание
- Добавлена конфигурация для подключения к удаленной БД цен (PRICES_DB_URI, PRICES_DB_URI_ASYNC)
- Реализован полный CRUD для работы с ценами через REST API
- Добавлена обработка ошибок и fallback для случаев недоступности БД цен
- Интеграция с существующей архитектурой проекта без breaking changes
- Поддержка как синхронных, так и асинхронных операций с БД цен

## [2024-12-20] - Исправлена функциональность страницы управления товаром

### Исправлено
- Исправлена ошибка SQLAlchemy ArgumentError в модели ProductAllegroSyncSettings
- Устранена проблема с неправильным определением relationship в модели
- Исправлена маршрутизация для страницы управления товаром (/products/{sku}/manage)
- Разделены роутеры: каталог доступен по /catalog, управление товарами по /products/{sku}/manage
- Исправлена логика отображения остатков - теперь показываются все склады, даже с нулевыми остатками
- Исправлены API эндпоинты для тоглов синхронизации (убран лишний /v1 из URL)
- Добавлена кнопка "Применить" для мультипликатора цены вместо автоматического обновления

### Улучшено
- Теперь отображаются все склады из enum Warehouses, даже если для товара нет записей в таблице stock
- Улучшена UX - мультипликатор цены применяется по кнопке, а не автоматически при изменении

### Техническое описание
- Убран неправильный параметр foreign_keys из определения relationship в ProductAllegroSyncSettings
- Создан отдельный catalog_router для каталога без префикса
- Исправлена логика формирования stock_by_warehouse для показа всех складов
- Исправлены URL в JavaScript: заменен /api/v1/ на /api/
- Заменен onchange на кнопку "Применить" для мультипликатора

## [2024-12-20] - Исправлен круговой импорт в модулях Celery

### Исправлено
- Решена проблема кругового импорта между app/celery_app.py, app/services/warehouse/manager.py и app/services/allegro/sync_tasks.py
- Создан отдельный модуль app/celery_shared.py для общих объектов Celery
- Перенесены celery, SessionLocal, get_allegro_token в общий модуль
- Обновлены импорты в зависимых модулях

### Улучшено
- Улучшена архитектура модулей - устранена циклическая зависимость
- Разделены обязанности: celery_shared.py содержит только общие объекты, celery_app.py - только задачи
- Повышена стабильность запуска приложения

### Техническое описание
- Создан app/celery_shared.py с инициализацией celery и общими функциями
- Обновлен app/celery_app.py - убраны общие объекты, оставлены только задачи
- Обновлен app/services/allegro/sync_tasks.py - изменен импорт с celery_app на celery_shared
- Исправлена ImportError: cannot import name 'celery' from partially initialized module

## [2024-12-20] - Исправлена работа совместного использования фильтров по остаткам

### Исправлено
- Исправлена проблема с одновременным использованием фильтров по минимальному и максимальному количеству
- Объединение условий фильтрации в один вызов .having() с логическим AND
- Исправлена логика JavaScript для передачи параметров фильтров на сервер
- Добавлена автоматическая коррекция некорректных значений фильтров (когда max < min)
- Теперь фильтры "Остаток меньше чем" и "Остаток больше чем" работают корректно вместе

### Улучшено
- Улучшен пользовательский интерфейс фильтров: заменены символы "< 999" и "> 999" на понятные "Макс." и "Мин."
- Добавлены математические символы ≤ и ≥ над полями фильтров для лучшего понимания
- Добавлены подсказки (title) для полей фильтров
- Увеличена ширина полей фильтров для лучшего восприятия

### Техническое описание
- Заменены отдельные вызовы .having() на единый вызов с объединенными условиями
- Добавлена логика сбора условий фильтрации в массив having_conditions
- Использование sqlalchemy.and_() для корректного объединения условий
- Исправлен JavaScript код в handleFilterChange() и goToPage() для правильной передачи параметров
- Параметры stock_filter и min_stock_filter теперь отправляются только когда поля заполнены
- Убрана проверка >= 0 в бэкенде, поскольку пустые поля больше не отправляются как -1
- Добавлена валидация параметров фильтров с автоматической коррекцией некорректных значений

## [2024-12-20] - Исправлена ошибка 405 Method Not Allowed в API Allegro и добавлена таблица блокировок
### Исправлено
- Ошибка 405 Method Not Allowed при обновлении остатков офферов Allegro
- Создан новый метод update_offer_stock() использующий правильный API endpoint
- Заменен PATCH запрос на PUT /sale/offer-quantity-change-commands/{commandId}
- Добавлена миграция для создания таблицы productsynclock
- Исправлена ошибка "relation productsynclock does not exist"

### Предыдущие исправления
- TypeError: InventoryManager.__init__() missing 2 required positional arguments: 'dsn' and 'async_dsn'
- Заменен прямой вызов InventoryManager() на get_manager() в sync_tasks.py
- Обновлены импорты для корректной работы с менеджером инвентаря
- Исправлен вызов несуществующего метода get_total_stock() на get_stock_by_sku() с суммированием остатков

### Предыдущие изменения
- Ошибка AttributeError: 'str' object has no attribute 'get' в sync_allegro_offers_batch
- Корректная обработка ответа API get_offers (извлечение списка офферов из ответа)
- Улучшено логирование процесса синхронизации с детальной информацией о типах данных
- Детальное логирование в sync_allegro_offers_batch для отладки
- Проверка типов данных в процессе синхронизации
- Защита от некорректных данных с информативными сообщениями об ошибках
- Логирование каждого этапа обработки офферов

## [2024-12-20] - Добавлены расширенные фильтры каталога товаров
### Добавлено
- Фильтр "Остаток больше чем" для минимального количества товаров
- Отдельный фильтр по бренду через выпадающий список
- API endpoint GET /api/products/brands для получения списка уникальных брендов
- Поддержка параметра brand_filter в эндпоинтах каталога
- Поддержка параметра min_stock_filter в эндпоинтах каталога
- Динамическая загрузка списка брендов при инициализации страницы

### Изменено
- Десктопная версия каталога использует кастомную сетку фильтров с оптимизированными размерами
- Поле поиска максимально расширено для удобства ввода (поиск только по названию, SKU, EAN)
- Поля фильтрации остатков компактные (80px) с символами < и > в плейсхолдерах
- Ограничение ввода до 3 символов для полей остатков с центрированием текста
- Мобильная версия включает новые поля фильтрации с аналогичными изменениями
- JavaScript функции обновлены для поддержки новых фильтров
- Функция getActiveFormElements включает новые поля фильтрации

### Удалено
- Поиск по бренду из строки поиска (теперь только через отдельный фильтр)

## [2024-12-20] - Добавлено поле редактирования бренда товара и поиск по бренду
### Добавлено
- Поле "Бренд" в форму редактирования товара (edit_product.html)
- Обработка параметра brand в API endpoint PUT /api/products/{sku}
- Валидация поля бренда: пустая строка преобразуется в NULL
- Логирование изменений бренда в операциях редактирования товара
- Поиск по полю бренда в каталоге товаров (Product.brand.ilike)

### Изменено
- Форма редактирования товара теперь включает поле для изменения бренда
- API endpoint обновления товара поддерживает изменение бренда
- Данные для формы редактирования включают текущее значение бренда
- Поисковая строка теперь ищет по названию, SKU, EAN и бренду
- Обновлены placeholder'ы в поисковых полях каталога

## [2024-12-20] - Добавлена синхронизация одного товара по всем аккаунтам Allegro
### Добавлено
- Celery задача sync_allegro_stock_single_product для синхронизации одного товара по всем аккаунтам
- API endpoint POST /api/products/{sku}/sync-allegro для запуска синхронизации товара
- Кнопка "Синхронизировать" в карточке товара для ручного запуска синхронизации
- JavaScript функция syncProductAllegro для обработки клика на кнопку синхронизации
- Проверка существования товара в БД перед синхронизацией
- Проверка флага is_sync_enabled для товара перед синхронизацией
- Обновлено описание файла sync_tasks.py для отражения новой функциональности

### Изменено
- Обновлены dependencies в описании модуля sync_tasks.py
- Улучшена структура блока синхронизации в карточке товара
- Кнопка синхронизации отображается только при включенном флаге is_sync_enabled

## [2024-12-20] - Реализация UI управления синхронизацией товаров
### Добавлено
- Подпись "Allegro:" перед toggle переключателем синхронизации
- Подробный tooltip с описанием функции синхронизации
- API endpoint PATCH /api/products/{sku}/sync-flag для переключения флага синхронизации
- Новый тип операции PRODUCT_SYNC_TOGGLE для логирования изменений флага
- Метод create_product_sync_toggle_operation в OperationsService
- JavaScript функция toggleSyncFlag с обновлением UI в реальном времени

### Изменено
- Улучшен UI карточки товара: добавлена подпись и детальный tooltip
- Обновлена структура toggle элементов для лучшей читаемости

## [2024-06-13] - Этапы реализации автоматической синхронизации остатков с Allegro
### Завершено
- Реализован пайплайн Celery задач для массовой синхронизации остатков
- Добавлен rate limiter для соблюдения лимитов API Allegro
- Реализована система уведомлений об ошибках в Telegram
- Добавлено детальное логирование и мониторинг
- Добавлен булевый флаг is_sync_enabled в модель товара и миграция

### В процессе
- Обновление бизнес-логики синхронизации: сначала is_sync_enabled, затем логи с локами
- Реализация и интеграция механизма блокировок (локов) для защиты от дублирующихся обновлений по SKU

### Запланировано
- Кнопка в UI карточки товара для ручного включения/отключения синхронизации
- Интеграция задачи в планировщик Celery Beat 

## [2024-12-19] - Переработка системы синхронизации товаров с Allegro
### Добавлено
- Новая модель `ProductAllegroSyncSettings` для индивидуальных настроек синхронизации по аккаунтам
- Страница управления товаром `/products/{sku}/manage` с детальными настройками синхронизации
- API эндпоинты для управления настройками синхронизации:
  - `POST /api/v1/products/{sku}/sync-settings` - создание/обновление настроек
  - `GET /api/v1/products/{sku}/sync-settings` - получение всех настроек
  - `POST /api/v1/products/{sku}/sync-settings/{account_name}/toggle-stock` - переключение синхронизации остатков
  - `POST /api/v1/products/{sku}/sync-settings/{account_name}/toggle-price` - переключение синхронизации цен
  - `POST /api/v1/products/{sku}/sync-settings/{account_name}/update-multiplier` - обновление мультипликатора цены
- Возможность настройки мультипликатора цены для каждого аккаунта Allegro отдельно
- Отдельные настройки синхронизации остатков и цен для каждого аккаунта
- Красивый интерфейс управления настройками синхронизации с переключателями и полями ввода

### Изменено
- Заменен тогл синхронизации на кнопку "Управление" в карточке товара
- Админы получают кнопку "Управление", обычные пользователи - кнопку "Просмотр"
- Настройки синхронизации теперь глобальные для всех пользователей системы
- Упрощена архитектура - убрана привязка к пользователю в настройках

### Удалено
- Старый JavaScript код для тогла синхронизации в product_card.html
- Зависимость от прямой привязки к токену в модели настроек

### Техническая информация
- Модель `ProductAllegroSyncSettings` использует составной ключ `product_sku + allegro_account_name`
- Подготовлена архитектура для будущего перехода на микросервисную систему
- Сохранена обратная совместимость с существующим полем `is_sync_enabled` в модели Product 

## [2024-12-30] - Исправление ссылок в уведомлениях о валидации заказов
### Исправлено
- Исправлены ссылки в Telegram уведомлениях о провале валидации заказов
- Теперь используется правильная ссылка на детали операции через operation_id вместо order_id
- Улучшено форматирование ссылок в HTML формате для лучшего отображения в Telegram

### Изменено
- Обновлен метод `notify_order_validation_failure` в `StockSyncNotificationService`
- Ссылки теперь ведут на страницу деталей операции (`/stock-sync/monitor/operation/{operation_id}`)
- Добавлен fallback на мониторинг по order_id, если operation_id недоступен

## [2024-12-28] - Добавление кнопок управления статусом списания на странице деталей операции
### Добавлено
- Кнопки "Списать", "Пометить списанным" и отображение текущего статуса списания рядом с кнопкой "Перейти к товарам"
- Функционал списания товаров из заказа прямо со страницы деталей операции
- Возможность пометить заказ как списанный без фактического списания со склада
- Отображение текущего статуса списания (Списано/Не списано) с цветовой индикацией
- Интеграция с существующими API endpoints для списания и пометки заказов

### Изменено
- Обновлен шаблон `operation_details.html` для отображения кнопок управления списанием
- Добавлена секция управления статусом списания в заголовке товаров операции
- Кнопки списания теперь доступны как на странице заказов Allegro, так и на странице деталей операции

### Улучшено
- Пользовательский опыт: возможность управлять статусом списания из разных разделов системы
- Единообразие интерфейса: одинаковый функционал на страницах заказов и деталей операции
- Визуальная индикация статуса списания для быстрого понимания состояния заказа

## [2025-01-13] - Исправление отображения статуса списания заказов

### Исправлено
- Убрана неправильная привязка к статусу операции при отображении статуса списания
- Теперь статус списания корректно загружается от микросервиса через флаг `is_stock_updated`
- Добавлено подробное логирование для отладки процесса загрузки и обновления статуса
- Исправлены ошибки линтера в JavaScript коде
- **Исправлен эндпоинт для получения статуса списания** - теперь используется `/api/allegro_sync/orders/{token_id}` вместо `/api/allegro_sync/orders/{token_id}/{order_id}`

### Технические изменения
- Убраны прямые обращения к шаблону в JavaScript коде
- Данные передаются через data-атрибуты HTML элементов
- Добавлено логирование в функции `loadStockStatus`, `updateStockStatus`, `saleFromOrder`, `markOrderStockUpdated`
- **Функция `loadStockStatus` теперь получает список всех заказов и ищет нужный по ID**, что позволяет корректно получить флаг `is_stock_updated`
- **Исправлены параметры запроса к эндпоинту** - добавлены обязательные параметры `limit` и `offset`
- **Исправлен конфликт маршрутов** - изменен маршрут с `/orders/{token_id}` на `/allegro-orders/{token_id}` для избежания конфликта с другими роутерами

## [2025-01-13] - Выявление критической проблемы с утечками сессий базы данных
### Выявлено
- Критическая проблема с утечками сессий базы данных, приводящая к ошибке "QueuePool limit of size 5 overflow 10 reached"
- Множественные места в коде где создаются сессии без гарантированного закрытия
- Использование `SessionLocal()` напрямую без контекстных менеджеров в Celery задачах
- Отсутствие единообразного подхода к управлению сессиями в сервисах

### Технические детали
- Ошибка возникает в микросервисе при получении токена: "QueuePool limit of size 5 overflow 10 reached, connection timed out, timeout 30.00"
- Проблема затрагивает все сервисы, использующие базу данных
- Требуется рефакторинг всех мест создания сессий для использования контекстных менеджеров
- Необходимо внедрить единый паттерн управления сессиями

### Приоритет
- КРИТИЧЕСКИЙ - требует немедленного исправления
- Влияет на стабильность всей системы
- Может привести к полной недоступности базы данных

## [2024-12-28] - Замена перезагрузки страницы на динамическое обновление данных операции
### Добавлено
- Функция `refreshOperationData()` для обновления данных операции без перезагрузки страницы
- Функция `refreshOrderData()` для обновления данных заказа Allegro
- Функция `updateOperationDisplay()` для перерисовки интерфейса с новыми данными
- API вызовы для получения актуальных данных операции и заказа
- Функция `forceRestoreAllButtons()` для принудительного восстановления заблокированных кнопок
- Глобальные обработчики ошибок для автоматического восстановления кнопок
- Таймауты восстановления кнопок как дополнительная защита

### Изменено
- Функция `saleFromOrder()`: заменена перезагрузка страницы на вызов `refreshOperationData()`
- Убрана задержка в 2 секунды перед перезагрузкой страницы
- Убрано изменение текста кнопок - оставлена только блокировка/разблокировка
- Исправлена область видимости переменной `button` для корректной работы блока `finally`
- Улучшена производительность: данные обновляются мгновенно без потери состояния страницы

### Улучшено
- Пользовательский опыт: отсутствие мерцания и потери позиции на странице
- Производительность: быстрее обновление данных без полной перезагрузки
- Сохранение состояния: пользователь остается на той же позиции страницы
- Надежность: кнопки автоматически восстанавливаются даже при ошибках
- Упрощение кода: убрана сложная логика изменения текста кнопок

## [2024-12-28] - Изменение кнопки "Товары" на toggle со стрелкой и удаление кнопки закрытия
### Изменено
- Кнопка "Товары" теперь работает как переключатель (toggle) со стрелкой
- Стрелка указывает вправо когда панель закрыта, влево когда открыта
- При клике на кнопку панель открывается/закрывается в зависимости от текущего состояния
- Убрана кнопка закрытия из панели быстрого редактирования товаров
- Обновлена логика сдвига основного контента (убрано 40px для кнопки закрытия)

### Удалено
- Кнопка закрытия `quickEditCloseBtn` из панели быстрого редактирования
- Функция `openProductsPanel` заменена на `toggleProductsPanel`

### Добавлено
- Новая функция `toggleProductsPanel` для переключения состояния панели
- Автоматическое изменение направления стрелки при открытии/закрытии панели
- Сброс направления стрелки при закрытии панели через Escape или другие способы

### Технические детали
- Кнопка "Товары" теперь имеет ID `productsToggleBtn` и иконку `productsToggleIcon`
- Стрелка использует SVG с классом `transition-transform duration-200` для плавной анимации
- Функция `closeQuickEdit` автоматически устанавливает направление стрелки влево (закрыто)
- Функция `openQuickEdit` устанавливает направление стрелки вправо (открыто)
- Начальное состояние стрелки: влево (панель закрыта)
- Основной контент теперь сдвигается на 50% вместо `calc(50% + 40px)`

### Улучшено
- Пользовательский опыт: единая кнопка для открытия/закрытия панели
- Визуальная обратная связь: стрелка показывает текущее состояние панели
- Консистентность: кнопка работает как стандартный toggle элемент
- Упрощение интерфейса: убрана лишняя кнопка закрытия

## [2024-12-28] - Улучшение логики получения полной цены заказа из fullOrder
### Добавлено
- Функция `updateOrderSummary` для обновления итогов заказа (стоимость доставки и общая сумма)
- Функция `updateOrderItemsTable` для динамического обновления таблицы товаров заказа
- Автоматическая инициализация данных заказа при загрузке страницы, если fullOrder доступен

### Изменено
- Логика отображения стоимости доставки: теперь берется из `full_order.delivery.cost` вместо `full_order.summary.totalToPay.delivery`
- HTML шаблон `order_items.html`: улучшена логика отображения итогов заказа
- Функция `updateOrderDisplay`: добавлен вызов `updateOrderSummary` для обновления итогов

### Технические детали
- Стоимость доставки получается из `fullOrder.delivery.cost.amount` и `fullOrder.delivery.cost.currency`
- Общая сумма получается из `fullOrder.summary.totalToPay.amount` и `fullOrder.summary.totalToPay.currency`
- Таблица товаров динамически обновляется через JavaScript при изменении данных заказа
- Добавлена обработка ошибок и логирование для всех функций обновления

### Улучшено
- Точность данных: цены теперь берутся из актуального источника (fullOrder)
- Динамическое обновление: таблица товаров и итоги обновляются без перезагрузки страницы
- Пользовательский опыт: данные заказа инициализируются сразу при загрузке страницы
- Надежность: добавлена проверка наличия данных и обработка ошибок

## [2024-12-28] - Расширение деталей операции: добавление блока с информацией о покупателе и адресе доставки
### Добавлено
- Новый блок с детальной информацией о покупателе (слева): имя, фамилия, email, телефон, логин
- Новый блок с информацией об адресе доставки (справа): город, улица, почтовый индекс, страна, способ доставки
- Разделительная линия между основными данными операции и новой информацией
- Отображение стоимости доставки и общей суммы заказа
- Информация о дате покупки и времени доставки
- Блок с детальной информацией о товарах в заказе: название, SKU, количество, цена
- Изменение заголовка на "Товары заказа"
- Кнопка закрытия в шапке для управления панелью товаров

### Технические детали
- Обновлен шаблон `operation_details_blocks/header.html`: добавлен новый блок после существующих данных
- Использованы данные из объекта `full_order` для отображения информации о покупателе и доставке
- Добавлена проверка наличия данных перед отображением для избежания ошибок
- Сохранена существующая структура и стилизация для консистентности интерфейса
- Основной блок с ID заказа, аккаунтом и статусами вынесен из сворачиваемой области и всегда отображается
- Новый блок с информацией о покупателе и доставке сворачивается/разворачивается вместе с остальными деталями
- Исправлено отображение основного блока в свернутом состоянии
- Изменено состояние по умолчанию на свернутое со стрелкой вниз
- Структура разделена на два блока: постоянный (основная информация) и сворачиваемый (детали покупателя и доставки)
- Обновлен шаблон `quick_edit_panel.html`: изменен заголовок на "Товары заказа" и добавлена кнопка закрытия для всех устройств

### Исправлено
- Убран блок с информацией о товарах в заказе
- Убрана информация о стоимости доставки и общей сумме заказа
- Убрана информация о времени доставки
- Исправлена ошибка "ReferenceError: editPrice is not defined" - добавлена проверка существования функций и создание заглушек
- Добавлена функция `ensureFunctionsExist` для предотвращения ошибок ReferenceError
- Добавлена защита от ошибок в HTML с помощью try-catch блоков

## [2025-01-16] - Рефакторинг интерфейса заказов Allegro
### Изменено
- Упрощен интерфейс заказов Allegro - убраны детали заказа, оставлены только карточки
- Карточки стали кликабельными для перехода к деталям заказа через существующую страницу operation details
- Первая строка: дата/время (российский формат), ID заказа по центру, кнопка "Детали заказа"
- Вторая строка: статус заказа (с приоритетом как в scripts.html), кнопка "Пометить списанным" (если !is_stock_updated)
- Убраны лишние детали: информация о покупателе, адрес доставки, товары, цена заказа
- Сохранена функциональность поиска и пагинации
- Кнопка "Детали заказа" ведет на `/stock-sync/monitor/operation-by-order/{order_id}`
- Кнопка "Детали заказа" стилизована как кнопка "Детали операции" (светло-фиолетовая)
- Убран статус "Не списано" - показывается либо "Списано", либо кнопка "Пометить списанным"

## [2024-12-28] - Добавление кнопки "Добавить товар" в блок ошибок валидации операций

### Добавлено
- Кнопка "Добавить товар" для каждого невалидного товара в блоке ошибок валидации
- Автоматическое предзаполнение формы создания товара данными из операции (SKU, название, требуемое количество)
- Автоматический возврат на страницу операции после успешного создания товара

### Технические детали
- Обновлен шаблон `validation_errors.html` для отображения кнопки добавления товара
- Модифицирована функция `getReturnUrl()` в `add_product.html` для корректного возврата на страницу операции
- Кнопка предзаполняет форму параметрами: sku, name, quantity
- Логика возврата использует `document.referrer` для определения исходной страницы
- Добавлено информативное уведомление о предзаполнении полей

### Улучшено
- Пользовательский опыт: возможность быстро создать недостающий товар прямо из ошибок валидации
- Эффективность: автоматическое предзаполнение формы экономит время пользователя
- Навигация: автоматический возврат на исходную страницу операции после создания товара
- Информативность: пользователь понимает, что произойдет после создания товара

## [2024-12-28] - Стилизация кнопок в блоке action_buttons: применение единого дизайна

### Изменено
- Кнопка "Провести операцию списания" перестилизована в зеленом стиле как кнопка "Запустить"
- Кнопка "Отменить операцию списания" перестилизована в красном стиле как кнопка "Остановить"
- Кнопка "Товары" перестилизована в сером стиле как кнопка "Подключить аккаунт"
- Убраны иконки круга, крестика и плюсика с кнопок для упрощения дизайна

### Технические детали
- Применены стили из `synchronization.html` для единообразия дизайна
- Удалены SVG иконки с кнопок "Провести операцию списания" и "Отменить операцию списания"
- Удалена иконка плюсика с кнопки "Товары"
- Сохранена стрелка-индикатор на кнопке "Товары" для функциональности сворачивания/разворачивания
- Обновлены CSS классы для соответствия общему стилю приложения
- Сохранена функциональность всех кнопок

### Улучшено
- Визуальная консистентность: все кнопки теперь используют единый стиль
- Пользовательский опыт: понятные цветовые ассоциации для действий
- Минимализм: убраны лишние иконки, оставлен только функциональный элемент (стрелка)
- Читаемость: текст кнопок стал более заметным без отвлекающих иконок

## [2024-12-28] - Исправление логики возврата на предыдущую страницу после создания товара

### Исправлено
- Улучшена функция `getReturnUrl()` для корректного определения URL возврата после создания товара
- Добавлена поддержка параметра `return_to` в дополнение к существующему `next`
- Исправлена логика работы с `document.referrer` для предотвращения зацикливания
- Добавлена проверка безопасности URL перед возвратом (URL должен начинаться с /)

### Добавлено
- Приоритетная система определения URL возврата:
  1. Параметр `next` из URL (высший приоритет)
  2. Параметр `return_to` из URL
  3. `document.referrer` (если не является текущей страницей)
  4. Автоматический возврат на страницу операций при наличии данных операции
  5. Возврат в каталог по умолчанию
- Уведомления об успешном создании товара с задержкой перед переходом
- Улучшенные уведомления об ошибках вместо стандартных alert
- Логирование в консоль для отладки процесса возврата

### Изменено
- Заменены стандартные `alert` на кастомные уведомления с лучшим UX
- Добавлена задержка в 1 секунду перед переходом для показа уведомления об успехе
- Улучшена обработка ошибок декодирования URL параметров
- Добавлена проверка домена для referrer URL

### Улучшено
- Пользовательский опыт: корректный возврат на страницу, с которой пришел пользователь
- Надежность: fallback механизмы для случаев, когда основной способ определения URL не работает
- Безопасность: проверка корректности URL перед переходом
- Отладка: подробное логирование процесса определения URL возврата

### Технические детали
- Функция `getReturnUrl()` теперь использует многоуровневую систему приоритетов
- Добавлена обработка ошибок для всех способов получения URL
- Улучшена совместимость с различными сценариями использования (операции, каталог, другие страницы)
- Сохранена обратная совместимость с существующими параметрами URL

## [2024-12-28] - Исправление логики отображения кнопки "Создать товар" в блоке валидации

### Исправлено
- Проблема с отображением кнопки "Создать товар" для уже существующих товаров
- Заменена логика проверки существования товара через флаг `product_exists` на прямую проверку через API продуктов
- Добавлена JavaScript функция для динамической проверки существования товара и обновления UI

### Изменено
- Обновлен шаблон `validation_errors.html` для использования JavaScript проверки существования товара
- Кнопка "Создать товар" теперь проверяется через эндпоинт `/api/products/{sku}/exists`
- Упрощена логика отображения: кнопка показывается только для действительно несуществующих товаров, без дополнительных уведомлений

### Технические детали
- Используется AJAX запрос к API продуктов для проверки существования товара
- Проверка выполняется при загрузке страницы для всех товаров в блоке валидации
- В случае ошибки API кнопка показывается по умолчанию (для безопасности)
- Добавлены CSS классы для управления видимостью элементов

### Улучшено
- Точность определения существования товара: теперь проверяется напрямую через базу данных
- Пользовательский опыт: кнопка "Создать товар" появляется только когда это действительно необходимо
- Надежность системы: устранена зависимость от неточных флагов в логах валидации

### Добавлено
- Новый API эндпоинт `/api/products/{sku}/exists` для проверки существования товара без требований авторизации
- Возвращает JSON ответ с полем `exists` и дополнительной информацией о товаре

## [2024-12-19] - Реализация первой задачи спецификации Кайро: расширение модели Product
### Добавлено
- Поле `first_publication_date` в модель Product для хранения даты первой публикации офферты на Allegro
- Индекс на поле `first_publication_date` для оптимизации запросов
- Сервис `PublicationUpdateService` для обновления данных о публикации через микросервисные API
- Celery задачи для автоматического и ручного обновления данных о публикации
- Pydantic схемы для API обновления данных о публикации
- Административный эндпоинт `/update-publication-dates/` для ручного запуска обновления
- Эндпоинт `/update-publication-dates/status/{task_id}` для отслеживания статуса задач

### Изменено
- Модель Product расширена полем `first_publication_date` с индексом
- Добавлены новые импорты в warehouse.py для работы с публикациями
- `PublicationUpdateService` переведен на синхронный код для совместимости с Celery
- API эндпоинты обновления публикаций переведены на синхронные функции

### Технические детали
- Реализована батчевая обработка по 100 SKU за раз
- Добавлена обработка ошибок API с exponential backoff
- Реализовано логирование процесса обновления
- Подготовлена архитектура для интеграции с микросервисом Allegro
- Соблюдены требования 1.4, 4.1, 4.2, 6.3 из спецификации
- Сервис использует `AllegroOffersMicroserviceClient` вместо `AllegroApiService`
- Celery задачи работают с синхронным сервисом без сложной логики event loop
- Правильно реализована обработка формата данных микросервиса оффертов
- Токены получаются с микросервиса через `AllegroTokenMicroserviceClient`
- Обработка ответа микросервиса учитывает структуру с результатами для каждого токена

### Следующие шаги
- Интеграция с реальным микросервисом Allegro для получения данных о публикации
- Реализация еженедельной задачи в Celery Beat
- Создание EnhancedReportService для генерации отчетов с данными о публикации

---

## [2024-12-19] - Реализация второй части спецификации Кайро - расширение генерации отчетов для включения данных о публикации

### Добавлено
- EnhancedReportService для автоматического получения недостающих данных о публикации при генерации отчетов
- Колонка "Дата первой публикации" в экспорте отчетов Excel
- Автоматическое обновление данных о публикации в базе при генерации отчетов
- Статистика покрытия данных о публикации

### Изменено
- InventoryManager.get_stock_report() расширен для включения поля first_publication_date
- Эндпоинты export_stock_with_sales и export_stock_with_sales_no_images используют EnhancedReportService
- Порядок колонок в отчетах обновлен - "Дата первой публикации" теперь последняя колонка
- Форматирование Excel файлов адаптировано для новой колонки
- Ширина колонки "Дата первой публикации" увеличена до 25 символов для полного отображения текста

### Исправлено
- Обработка NULL значений в поле first_publication_date
- Правильное форматирование дат в Excel отчетах
- Обратная совместимость с существующими форматами отчетов

### Технические детали
- EnhancedReportService автоматически определяет товары без данных о публикации
- Реализовано точечное получение недостающих данных через PublicationUpdateService
- Автоматическое сохранение полученных данных в базу для будущих отчетов
- Колонка "Дата первой публикации" перемещена в конец отчета для лучшей читаемости
- Ширина колонки с датой увеличена до 25 символов для полного отображения текста "Не опубликован"
- Форматирование дат в формате DD.MM.YYYY с fallback на "Не опубликован"

### Следующие шаги
- Настройка Celery Beat для еженедельного запуска задачи обновления данных о публикации
- Создание PublicationUpdateLimiter для предотвращения дублирования задач
- Тестирование и валидация всех новых компонентов

---

## [2024-12-19] - Добавление роута с базовой авторизацией для API docs

### Добавлено
- Новый роут `/api-docs/update-all-publication-dates/` для ручного запуска обновления дат публикации
- Базовая авторизация HTTP Basic для защиты API docs
- Настройки `API_DOCS_USERNAME` и `API_DOCS_PASSWORD` в конфигурации
- Функция `verify_basic_auth` для проверки учетных данных

### Изменено
- Конфигурация Settings расширена параметрами базовой авторизации
- Добавлена защита от несанкционированного доступа к API docs

### Технические детали
- Роут использует HTTP Basic авторизацию для простоты интеграции с API docs
- Учетные данные по умолчанию: username="admin", password="admin123"
- Роут запускает задачу `update_all_publication_dates_task` через Celery
- Возвращает детальную информацию о запущенной задаче включая ID и статус
- Логирование всех операций с указанием источника (API docs)

### Безопасность
- Использование `secrets.compare_digest` для безопасного сравнения паролей
- Защита от timing attacks при проверке учетных данных
- Возврат заголовка WWW-Authenticate при ошибке авторизации

---

## [2024-12-19] - Исправление ошибок обработки ответов микросервиса

### Исправлено
- Исправлена ошибка `'tuple' object has no attribute 'get'` в `_process_offers_response`
- Исправлено извлечение SKU из офферов - теперь правильно ищется в `external.id`
- Исправлено извлечение даты публикации - теперь правильно ищется в `publication.startedAt`
- Исправлена логика определения недостающих дат публикации - теперь корректно обрабатываются строки дат
- Улучшена обработка ответов микросервиса с проверкой типов данных
- Добавлено детальное логирование для отладки проблем с микросервисом
- Оптимизирован размер батча для предотвращения слишком длинных URL
- Оптимизирована логика обработки больших списков SKU

### Технические детали
- Исправлен неправильный URL в микросервисном клиенте (`/by-external-id` → `/by-external-ids`)
- Обновлен клиент для использования POST метода вместо GET (как требует микросервис)
- Добавлены параметры `limit` и `offset` в POST запрос для получения всех офферов
- Исправлена структура ответа - микросервис возвращает `List[Dict]` для каждого токена
- Исправлено извлечение SKU из поля `external.id` в структуре оффера Allegro
- Исправлено извлечение даты публикации из поля `publication.startedAt`
- Исправлена логика определения недостающих дат - теперь корректно обрабатываются строки дат из `InventoryManager`
- Улучшена обработка ошибок с детальным логированием типов данных
- Добавлены проверки типов на каждом уровне обработки ответа микросервиса

## [2024-12-19] - Добавление полей name_eng и brand для товаров
### Добавлено
- Поле `name_eng` (английское название) для товаров как опциональное
- Поле `brand` (бренд) для товаров как опциональное
- Обновлены формы создания и редактирования товаров
- Обновлены API эндпоинты для работы с новыми полями

### Изменено
- Форма добавления товара: добавлены поля для английского названия и бренда
- Форма редактирования товара: добавлено поле для бренда
- API эндпоинт создания товара: поддержка новых полей

## [2025-01-15] - Начало разработки системы уведомлений о продажах по низким ценам

### Добавлено
- Анализ спецификации kiro по системе уведомлений о продажах по цене ниже минимальной
- План реализации из 10 задач согласно требованиям и дизайну
- Обновление документации проекта (tasktracker.md, changelog.md)

### Планируется
- Создание модели данных LowPriceViolation с computed fields
- Реализация сервиса проверки цен LowPriceSaleChecker
- Расширение StockSyncNotificationService для уведомлений о нарушениях цен
- Интеграция проверки цен в StockSynchronizationService
- Добавление новых действий в StandardizedLogger
- Написание тестов для всех компонентов
- Добавление конфигурационных настроек

### Технические детали
- Система будет интегрироваться в существующий процесс обработки pending stock operations
- Проверка цен будет выполняться асинхронно без блокировки основных процессов
- Использование существующих сервисов: PricesService, StockSyncNotificationService, StandardizedLogger
- Поддержка graceful degradation при ошибках сервисов

## [2025-01-15] - Реализация системы уведомлений о продажах по низким ценам - этап 1

### Добавлено
- Модель данных `LowPriceViolation` с computed fields для нарушений минимальных цен
- Модель данных `LowPriceViolationSummary` для сводки нарушений по заказу
- Сервис `LowPriceSaleChecker` для проверки цен продаж на соответствие минимальным ценам
- Новые действия в `LogAction` для логирования проверки цен:
  - `PRICE_CHECK_STARTED` - начало проверки цен
  - `PRICE_CHECK_COMPLETED` - завершение проверки цен
  - `PRICE_VIOLATION_DETECTED` - обнаружение нарушения минимальной цены
  - `PRICE_CHECK_FAILED` - ошибка проверки цен
  - `PRICE_SERVICE_UNAVAILABLE` - недоступность сервиса цен
- Метод `notify_low_price_sales` в `StockSyncNotificationService` для отправки уведомлений о нарушениях

### Технические детали
- Модели используют Pydantic v2 с computed fields для автоматического вычисления суммы и процента нарушений
- Сервис проверки цен интегрируется с существующим `PricesService` для получения минимальных цен
- Поддержка graceful degradation: при недоступности сервиса цен проверка пропускается
- Асинхронная обработка без блокировки основных процессов
- Детальное логирование всех этапов проверки цен
- Форматированные уведомления с деталями нарушений, рекомендациями и ссылками на мониторинг

### Следующие шаги
- Интеграция проверки цен в `StockSynchronizationService`
- Добавление конфигурационных настроек
- Написание тестов для всех компонентов

## [2025-01-15] - Реализация системы уведомлений о продажах по низким ценам - завершение основных задач

### Добавлено
- Интеграция проверки цен в `StockSynchronizationService` в методе `_validate_and_deduct_stock`
- Конфигурационные настройки для проверки цен в `stock_sync_config`:
  - `price_checking_enabled` - включение/отключение проверки цен
  - `price_check_timeout_seconds` - таймаут проверки цен
  - `price_violation_threshold_percent` - порог нарушений
  - `price_check_batch_size` - размер батча для получения цен
- Автоматическая проверка цен продаж после валидации остатков
- Graceful degradation: при ошибках проверки цен основной процесс продолжается

### Изменено
- Обновлен `StockSynchronizationService` для использования `LowPriceSaleChecker`
- Добавлена инициализация `PricesService` и `LowPriceSaleChecker` в конструктор
- Метод `_validate_and_deduct_stock` теперь включает проверку цен перед списанием остатков

### Технические детали
- Проверка цен выполняется только при включенной конфигурации `price_checking_enabled`
- При обнаружении нарушений автоматически отправляются уведомления через `StockSyncNotificationService`
- Все ошибки проверки цен логируются, но не прерывают основной процесс списания остатков
- Система полностью интегрирована в существующий процесс обработки pending stock operations

### Статус
Основная функциональность системы уведомлений о продажах по низким ценам реализована и интегрирована.
Осталось написать тесты и создать документацию по использованию.

## [2025-01-15] - Завершение системы уведомлений о продажах по низким ценам

### Добавлено
- Полная документация по использованию системы уведомлений о продажах по низким ценам
- Руководство по конфигурации, интеграции и troubleshooting
- Примеры использования и расширения функциональности

### Завершено
- ✅ Модель данных `LowPriceViolation` с computed fields
- ✅ Сервис `LowPriceSaleChecker` для проверки цен продаж
- ✅ Расширение `StockSyncNotificationService` для уведомлений о нарушениях
- ✅ Интеграция в `StockSynchronizationService` в методе `_validate_and_deduct_stock`
- ✅ Новые действия в `LogAction` для логирования проверки цен
- ✅ Конфигурационные настройки в `stock_sync_config`
- ✅ Подробная документация по использованию

### Технические достижения
- Система полностью интегрирована в существующий процесс обработки pending stock operations
- Автоматическая проверка цен продаж после валидации остатков
- Graceful degradation при ошибках сервисов
- Структурированные уведомления с деталями нарушений и рекомендациями
- Конфигурируемость через переменные окружения

### Результат
Система уведомлений о продажах по низким ценам готова к использованию и обеспечивает:
- Автоматический мониторинг ценовой политики
- Оперативное реагирование на потенциальные убытки
- Интеграцию с существующей инфраструктурой уведомлений
- Надежность и производительность

## [2025-01-15] - Исправление проблем с логированием и интеграцией проверки цен

### Исправлено
- Ошибка `'StockSynchronizationService' object has no attribute 'standardized_logger'` - исправлен порядок инициализации в конструкторе
- Ошибка `'StockSynchronizationService' object has no attribute '_get_operations_for_processing'` - убран вызов несуществующего метода
- Ошибка `'StockSynchronizationService' object has no attribute '_log_operation'` - заменен на правильный метод логирования
- Заменены все `print` на правильное логирование через Python logging

### Добавлено
- Интеграция проверки цен продаж в API endpoint `/sale-from-order/` где происходит реальное списание товаров
- Отладочное логирование во все ключевые методы для диагностики проблем
- Проверка цен выполняется после успешного списания товаров через кнопку "Списать"

### Технические детали
- Проверка цен теперь интегрирована в правильное место - API endpoint списания товаров
- Все отладочные логи ведут через уровень INFO для лучшей видимости
- Исправлены все ошибки атрибутов и методов в StockSynchronizationService
- Система проверки цен работает независимо от основного процесса синхронизации

### Результат
Система уведомлений о продажах по низким ценам теперь корректно интегрирована и должна работать при списании товаров через кнопку "Списать".

## [2025-01-15] - Интеграция получения имени аккаунта от микросервиса

### Добавлено
- Получение имени аккаунта от микросервиса по токену в endpoint `/sale-from-order/`
- Интеграция с `AllegroTokenMicroserviceClient` для корректного отображения имени аккаунта в уведомлениях
- Отладочное логирование процесса получения имени аккаунта

### Технические детали
- Создается `AllegroTokenMicroserviceClient` с JWT токеном для аутентификации
- Вызывается метод `get_token(UUID(token_id))` для получения информации о токене
- Извлекается поле `account_name` из `TokenResponse`
- Имя аккаунта используется в уведомлениях о нарушениях минимальных цен
- Добавлена обработка ошибок с fallback на "Unknown(token_id)"

### Результат
Теперь уведомления о продажах по низким ценам будут содержать корректное имя аккаунта вместо "Unknown", что улучшит читаемость и понимание уведомлений.

## [2025-01-15] - Исправление ошибки с sync_session в проверке цен

### Исправлено
- Ошибка `local variable 'sync_session' referenced before assignment` - перемещен блок проверки цен внутрь блока `with SessionLocal() as sync_session:`
- Проверка цен теперь выполняется в правильном контексте с доступом к синхронной сессии базы данных

### Технические детали
- Блок проверки цен перемещен внутрь блока `with SessionLocal() as sync_session:` где создается переменная `sync_session`
- Это обеспечивает корректный доступ к базе данных для создания notification_service и standardized_logger
- Проверка цен выполняется после завершения операции синхронизации, но в том же контексте сессии

### Результат
Система проверки цен теперь корректно работает с доступом к базе данных и должна выполнять все операции без ошибок.

---

## [2025-01-15] - Добавление детального логирования для диагностики ошибки 500

### Добавлено
- Детальное логирование всех этапов выполнения endpoint `/sale-from-order/`
- Логирование создания операции, обновления статуса в микросервисе
- Логирование поиска и обновления операции синхронизации
- Логирование завершения всех блоков для отслеживания места возникновения ошибки

### Технические детали
- Добавлены логи уровня INFO для отслеживания прогресса выполнения
- Добавлены логи уровня ERROR с полными traceback для критических ошибок
- Логирование разделено по блокам: создание операции, обновление статуса, завершение синхронизации, проверка цен
- Каждый блок логирует свое начало и завершение для точной локализации проблемы

### Цель
Определить точное место возникновения ошибки 500 Internal Server Error при выполнении списания товаров.

---

## [2025-01-15] - Исправление ошибки с sync_session в проверке цен
### Исправлено
- Ошибка `local variable 'create_access_token' referenced before assignment` - перемещен импорт в начало функции
- Функция `create_access_token` теперь доступна во всех блоках кода
- Убраны дублирующиеся импорты для оптимизации

### Технические детали
- Импорт `create_access_token` и `settings` перемещен в начало функции `sale_from_order`
- Это обеспечивает доступность функций во всех блоках кода
- Убраны дублирующиеся импорты из блока проверки цен
- Функция теперь корректно создает JWT токены для всех необходимых сервисов

### Результат
Ошибка 500 Internal Server Error исправлена, система должна корректно выполнять списание товаров и проверку цен.

---

## [2025-01-15] - Добавление детального логирования для диагностики ошибки 500

### Добавлено
- Детальное логирование всех этапов выполнения endpoint `/sale-from-order/`
- Логирование создания операции, обновления статуса в микросервисе
- Логирование поиска и обновления операции синхронизации
- Логирование завершения всех блоков для отслеживания места возникновения ошибки

### Технические детали
- Добавлены логи уровня INFO для отслеживания прогресса выполнения
- Добавлены логи уровня ERROR с полными traceback для критических ошибок
- Логирование разделено по блокам: создание операции, обновление статуса, завершение синхронизации, проверка цен
- Каждый блок логирует свое начало и завершение для точной локализации проблемы

### Цель
Определить точное место возникновения ошибки 500 Internal Server Error при выполнении списания товаров.

---

## [2025-01-15] - Улучшение форматирования уведомлений о нарушениях цен

### Улучшено
- Убрано дублирование времени в уведомлениях о нарушениях минимальных цен
- Убрана ссылка на мониторинг операций из уведомлений
- Упрощен формат уведомлений для лучшей читаемости

### Технические детали
- Временная метка теперь добавляется только один раз в методе `_format_message`
- Убрана ссылка на мониторинг из `notify_low_price_sales`
- Сохранена вся важная информация: детали нарушений, рекомендации, общий убыток
- Уведомления стали более лаконичными и информативными

### Результат
Уведомления о нарушениях минимальных цен теперь имеют чистый, недублированный формат без лишних ссылок.

---

## [2025-01-15] - Исправление ошибки с create_access_token

### Исправлено
- Исправлена ошибка `'str' object has no attribute 'get'` в функции `sale_from_order`
- Добавлена проверка на наличие `create_access_token` перед использованием
- Добавлена обработка ошибок для случаев, когда `create_access_token` не найден

### Технические детали
- Добавлены проверки на наличие `create_access_token` в функциях `sale_from_order` и `sale_from_order_async`
- Добавлена обработка ошибок для случаев, когда `create_access_token` не найден

## [2025-01-19] - Исправление отображения статуса операции в деталях операции

### Исправлено
- Исправлено отображение статуса операции: добавлен статус 'stock_deducted' (Списано) с соответствующим стилем
- Добавлен статус 'cancelled' (Отменена) с красным стилем для отмененных операций
- **Критическое исправление**: проблема решена на уровне Python кода - статус операции теперь преобразуется в строку перед передачей в шаблон
- **Дополнительное исправление**: исправлена проблема с JavaScript обновлением статуса - в hidden_data.html использовался raw enum вместо строки
- **Исправление наложения стилей**: устранена проблема с наложением цветов статусов при AJAX обновлениях
- Убрана отладочная информация из интерфейса
- Устранен fallback на raw enum значения в блоке else

### Технические детали
- Добавлен статус 'stock_deducted' с синим стилем (bg-blue-100 text-blue-800)
- Добавлен статус 'cancelled' с красным стилем (bg-red-100 text-red-800)
- Обновлены условные блоки для корректного отображения всех возможных статусов операции
- Статус 'Списано' теперь отображается корректно вместо raw значения
- **Решение на уровне Python**: в роутере `stock_sync.py` добавлено преобразование `operation.status.value` в строку
- В шаблоне теперь используется переменная `operation_status_str` вместо `operation.status`
- **Решение на уровне JavaScript**: в `hidden_data.html` заменен `{{ operation.status }}` на `{{ operation_status_str }}`
- Обновлены обе функции: `operation_details_page` и `operation_details_by_order_page`
- В JavaScript функции `updateOperationStatus` добавлен недостающий статус `stock_deducted`
- **Исправление наложения стилей**: добавлена проверка на изменение статуса и очистка CSS классов перед применением новых стилей
- Убран дублирующий вызов `updateOperationStatus` в `refreshOperationData`

### Улучшено
- Читаемость: статусы операций теперь отображаются на русском языке
- Визуальное восприятие: каждый статус имеет соответствующий цвет и стиль
- Консистентность: все статусы из модели OperationStatus корректно обрабатываются
- Надежность: устранена проблема с отображением raw enum значений типа "OperationStatus.COMPLETED"
- Пользовательский опыт: убрана отладочная информация из интерфейса
- Архитектура: решение применено на правильном уровне (Python код, а не шаблон)
- JavaScript интеграция: статус корректно обновляется через AJAX без перезагрузки страницы
- **Производительность**: устранены дублирующие обновления статуса и наложение стилей

## [2025-01-19] - Адаптация существующего воркспейса для работы на всех страницах сайта

### Изменено
- Адаптирован существующий воркспейс для работы на всех страницах сайта
- Обновлена функция `toggleWorkspace()` для универсального поиска контента страницы
- Интегрирован воркспейс в `base.html` для глобальной доступности
- Улучшена логика определения основного контента страницы

### Технические детали
- Функция `toggleWorkspace()` теперь ищет контент по нескольким селекторам:
  - `#mainContent` (оригинальный селектор для каталога)
  - `main` (семантический HTML5 элемент)
  - `.main-content` (класс для основного контента)
  - `.content` (общий класс контента)
  - `body` (fallback на весь документ)
- Сохранена вся существующая функциональность воркспейса
- Добавлена обработка случаев, когда основной контент не найден

### Функциональность
- **Универсальность**: работает на любой странице сайта
- **Автоматическое копирование**: копирует контент текущей страницы в правую панель
- **Сохранение функций**: все существующие функции воркспейса работают без изменений
- **Обратная совместимость**: полностью совместим с существующим кодом каталога

### Улучшено
- Доступность: воркспейс теперь доступен на всех страницах сайта
- Простота: используется существующий код без дублирования
- Надежность: улучшена обработка различных структур страниц
- Производительность: минимальные изменения в существующем коде

## [2025-01-19] - Добавление стилей выделения карточек с синеватым дизайном

### Добавлено
- Добавлены стили для выделения карточек товаров в воркспейсе
- Синеватый фон (#eff6ff - blue-50) для выделенных карточек
- Синяя граница (#3b82f6 - blue-500) с увеличенной толщиной (2px) для выделенных карточек
- Базовые стили карточки продукта скопированы из шаблона для консистентности

### Технические детали
- Стили добавлены в `scripts.html` через JavaScript для динамического применения
- Использован класс `.product-card.workspace-selected` для выделенного состояния
- Все остальные стили карточки остаются неизменными при выделении
- Сохранены все исправления позиционирования кнопок управления

### Улучшено
- Визуальная обратная связь: четкое выделение выбранных товаров
- Консистентность дизайна: стили основаны на существующих стилях карточки
- Пользовательский опыт: интуитивно понятное выделение синим цветом

## [2025-01-19] - Принудительное исправление позиционирования кнопок управления

### Исправлено
- Принудительное исправление позиционирования кнопок редактирования и удаления с использованием !important
- Добавлены inline стили для гарантированного применения позиционирования
- Исправлены конфликты CSS с использованием более специфичных селекторов
- Добавлены дополнительные стили прямо в шаблон карточки для принудительного исправления

### Технические детали
- Добавлены inline стили `position: absolute !important; z-index: 20 !important;` для контейнера кнопок
- Добавлены inline стили `position: relative !important; z-index: 21 !important;` для самих кнопок
- Добавлены дополнительные CSS стили в шаблон с !important для принудительного применения
- Обновлена версия скрипта до 2025-01-19-v7 для принудительного обновления кэша
- Использованы более специфичные селекторы `.product-card .absolute.bottom-4.left-4`

### Улучшено
- Принудительное позиционирование: кнопки теперь гарантированно отображаются в правильном месте
- Z-index иерархия: четкая иерархия слоев с высокими значениями z-index
- Совместимость: исправлены все возможные конфликты CSS стилей
- Надежность: использование !important гарантирует применение стилей

## [2025-01-19] - Исправление наложения кнопок редактирования и удаления на блок складов

### Исправлено
- Исправлено наложение кнопок редактирования и удаления на блок остатков по складам
- Исправлены конфликты CSS стилей между глобальными стилями и абсолютным позиционированием кнопок
- Улучшено z-index управление для кнопок с абсолютным позиционированием
- Исправлена проблема с позиционированием кнопок в воркспейсе

### Технические детали
- В scripts.html исправлены CSS селекторы для кнопок - теперь не переопределяется position для кнопок с классом "absolute"
- Добавлен специальный z-index: 10 для кнопок с абсолютным позиционированием в глобальных стилях
- В product_card.html добавлен z-20 для контейнера кнопок управления
- Добавлены shadow-sm и hover:shadow-md для лучшего визуального эффекта кнопок
- Обновлена версия скрипта до 2025-01-19-v6 для принудительного обновления кэша

### Улучшено
- Позиционирование: кнопки редактирования и удаления теперь корректно отображаются в левом нижнем углу
- Z-index управление: правильная иерархия слоев для всех элементов карточки
- Визуальное восприятие: кнопки имеют тени и hover эффекты
- Совместимость: исправлены конфликты между глобальными стилями и локальными стилями карточки

## [2025-01-19] - Исправление проблемы с выбором товаров в воркспейс при закрытом воркспейсе

### Исправлено
- Исправлена критическая проблема с невозможностью выбора товаров в воркспейс при закрытом воркспейсе
- Вынесен глобальный обработчик кликов за пределы класса SimpleWorkspaceManager для обеспечения работы на всех страницах
- Добавлена глобальная функция `initializeWorkspace()` для автоматической инициализации воркспейса при необходимости
- Добавлена автоматическая инициализация воркспейса при первом клике по карточке товара
- Обновлены все функции воркспейса для автоматической инициализации при вызове
- Исправлена логика обработки кликов по карточкам товаров вне открытого воркспейса
- Добавлено детальное логирование кликов по карточкам товаров для отладки
- **НОВОЕ**: Добавлены прямые обработчики кликов `onclick="handleProductCardClick(event, this)"` в карточки товаров
- **НОВОЕ**: Создана функция `handleProductCardClick()` для обработки кликов по карточкам с проверкой интерактивных элементов
- **КРИТИЧЕСКОЕ**: Исправлена ошибка "redeclaration of let SimpleWorkspaceManager" - убрано дублирующее подключение воркспейса
- **ОТЛАДКА**: Улучшена функция `handleProductCardClick()` с детальным логированием и более точными проверками интерактивных элементов
- **КРИТИЧЕСКОЕ**: Исправлена логика проверки `[onclick]` - теперь не блокирует клики по самой карточке товара
- **ОТЛАДКА**: Добавлено детальное логирование в методы `toggleProduct` и `addProduct` для отладки проблемы с переключением товаров
- **КРИТИЧЕСКОЕ**: Исправлено дублирование обработчиков кликов - убран глобальный обработчик кликов по карточкам товаров
- **ИСПРАВЛЕНО**: Исправлен метод HTTP для массового удаления товаров - изменен с `POST` на `DELETE`
- **ИСПРАВЛЕНО**: Исправлен endpoint для массового удаления товаров - добавлен `Body(...)` для корректного приема массива SKU
- **ОТЛАДКА**: Добавлено детальное логирование в endpoint массового удаления товаров для отладки проблем
- **ИСПРАВЛЕНО**: Изменен формат запроса для массового удаления товаров - теперь используется модель Pydantic `BulkDeleteRequest`
- **ДОБАВЛЕНО**: Перерисовка каталога после успешного удаления товаров из системы
- **ДОБАВЛЕНО**: Автоматическая прокрутка каталога вверх после удаления товаров

### Технические детали
- Воркспейс теперь инициализируется автоматически при любом взаимодействии с товарами
- Глобальный обработчик кликов работает независимо от состояния воркспейса
- Упрощена логика setupEventListeners() в классе SimpleWorkspaceManager
- Все функции воркспейса теперь вызывают `initializeWorkspace()` перед выполнением операций
- **НОВОЕ**: Карточки товаров теперь имеют прямые обработчики кликов, которые работают независимо от других элементов
- **НОВОЕ**: Функция `handleProductCardClick()` проверяет, что клик не по интерактивным элементам (кнопки, поля ввода, ссылки)
- **КРИТИЧЕСКОЕ**: Убрано дублирующее подключение `workspace.html` из `catalog_new.html` (остается только в `base.html`)
- **ОТЛАДКА**: Добавлено детальное логирование в функцию `handleProductCardClick()` для отладки проблем с кликами
- **ОТЛАДКА**: Улучшены проверки интерактивных элементов - убрана проверка `.cursor-pointer` которая блокировала клики
- **КРИТИЧЕСКОЕ**: Исправлена логика проверки `[onclick]` - теперь исключает саму карточку товара из проверки
- **КРИТИЧЕСКОЕ**: Убран глобальный обработчик кликов по карточкам товаров - теперь используется только прямой обработчик `onclick`

## [2025-01-19] - Добавление функциональности удаления выделенных товаров из системы

### Добавлено
- Новый API endpoint `/api/v1/products/bulk-delete` для массового удаления товаров
- Кнопка "Удалить выделенные товары из системы" в меню действий воркспейса
- Детальное подтверждение удаления с предупреждением о необратимости действия
- Дополнительное подтверждение для удаления более 10 товаров
- Автоматическая очистка воркспейса после успешного удаления товаров
- Отображение детального результата удаления с количеством успешных и неудачных операций
- Обработка ошибок удаления с показом деталей проблемных товаров

### Изменено
- Расширено меню действий воркспейса новым разделом "Управление товарами"
- Улучшена система подтверждения действий с детальными предупреждениями

## [2025-01-19] - Исправление отображения кнопок в карточках товаров

### Исправлено
- Исправлено отображение кнопок в правой части карточки товара - улучшена структура и отступы
- Исправлено расположение кнопок управления складом - добавлены правильные отступы и стили
- Улучшены стили кнопок "Списать" и "Пополнить" - добавлены hover эффекты и лучшая читаемость
- Исправлены кнопки редактирования и удаления в нижнем левом углу - изменены с rounded-full на rounded-lg
- Улучшена структура блока остатков по складам - добавлены hover эффекты и лучшие отступы
- Исправлено значение по умолчанию для input полей количества - установлено значение 1 вместо 0

### Технические детали
- В правой части карточки добавлен gap-3 между элементами для лучшего визуального разделения
- Кнопки управления складом получили улучшенные стили с rounded-md и font-medium
- Input поля получили улучшенные стили фокуса с focus:border-blue-500
- Кнопки в нижнем левом углу получили shadow-sm и hover:shadow-md для лучшего визуального эффекта
- Блок остатков по складам получил hover:shadow-sm для интерактивности

### Улучшено
- Визуальное восприятие: кнопки теперь лучше выделяются и имеют четкие границы
- Пользовательский опыт: улучшена интерактивность с hover эффектами
- Читаемость: улучшены отступы и структура элементов
- Консистентность: все кнопки теперь используют единый стиль rounded-lg

## [2025-01-19] - Очистка избыточных логов от прайсинга и валидации цен

### Исправлено
- Убраны избыточные логи из `PricesService.is_available()` - убраны детальные логи о состоянии подключений
- Убраны избыточные логи из `PricesService.get_prices_by_skus()` - убраны логи о количестве найденных записей и возвращаемых ценах
- Убраны избыточные логи из `LowPriceSaleChecker` - убраны детальные логи о процессе извлечения SKU, цен и названий продуктов
- Убраны избыточные логи из `StandardizedLogger` - убраны дублирующие логи в стандартный logger
- Сохранены все важные бизнес-логи через `log_action_with_timing`

### Технические детали
- В `PricesService.is_available()` убраны 3 избыточных логи
- В `PricesService.get_prices_by_skus()` убраны логи о вызове метода, количестве записей и возвращаемых ценах
- В `LowPriceSaleChecker` убраны логи о процессе извлечения данных, проверке цен и создании сводки нарушений
- В `StandardizedLogger` убраны дублирующие логи в стандартный logger, оставлены только записи в базу данных
- Сохранены все вызовы `log_action_with_timing` для отслеживания бизнес-событий

### Улучшено
- Читаемость логов: убрана избыточная техническая информация
- Производительность: уменьшено количество записей в лог-файлы
- Фокус на важном: логи содержат только критически важную информацию
- Сохранение бизнес-логики: все важные события по-прежнему логируются через `log_action_with_timing`

## [2024-12-19] - Добавлен роут для массовой синхронизации остатков всех аккаунтов

### Добавлено
- Новый API роут `POST /api/products/sync-all-accounts/stocks` для запуска синхронизации остатков всех аккаунтов Allegro
- Авторизация: доступ только для администраторов
- Логирование запуска массовой синхронизации
- Возврат task_id для отслеживания выполнения задачи

### Описание
Роут запускает существующую Celery-задачу `sync_allegro_stock_all_accounts`, которая:
- Получает все активные токены из микросервиса Allegro
- Для каждого аккаунта запускает синхронизацию только товаров с включенной синхронизацией остатков
- Использует групповые задачи Celery для параллельной обработки аккаунтов

## [2024-12-19] - Добавлена принудительная синхронизация остатков всех аккаунтов

### Добавлено
- Новая Celery-задача `sync_allegro_stock_all_accounts_force` для принудительной синхронизации остатков всех аккаунтов
- Новая Celery-задача `sync_allegro_stock_single_account_force` для принудительной синхронизации одного аккаунта
- Новый API роут `POST /api/products/sync-all-accounts/stocks/force` для запуска принудительной синхронизации
- Расширенное логирование с предупреждениями о принудительном режиме
- Флаг `force_mode: true` в ответе API для идентификации принудительной синхронизации

### Особенности
- **ИГНОРИРУЕТ** флаг включения синхронизации остатков в настройках ProductAllegroSyncSettings
- Синхронизирует **ВСЕ** товары для всех активных аккаунтов
- Предназначена для исключительных случаев (восстановление после сбоев, полная пересинхронизация)
- Может быть ресурсоемкой - требует осторожного использования
- Доступна только администраторам

## [2024-12-19] - Добавлены кнопки управления на страницу каталога

### Добавлено
- Кнопка "Выделить все" для выделения всех товаров на странице каталога
- Кнопка "Отменить выбор" для снятия выделения со всех товаров
- Кнопка "Принудительная синхронизация остатков" для запуска принудительной синхронизации (только для администраторов)
- JavaScript функциональность для управления выделением товаров
- Подтверждение действия при запуске принудительной синхронизации
- Индикатор загрузки при выполнении принудительной синхронизации

### Особенности
- Кнопки выделения работают только на текущей странице каталога
- Кнопка принудительной синхронизации доступна только администраторам
- При запуске принудительной синхронизации показывается предупреждение о ресурсоемкости операции
- Обе кнопки выделения ("Выделить все" и "Отменить выбор") всегда видны
- Уведомления о результатах операций

### Исправлено
- Кнопка "Отменить выбор" теперь всегда видна (убрано скрытие по умолчанию)
- Упрощена логика переключения кнопок выделения

### Улучшено
- Интеграция с воркспейсом: при выделении товаров они автоматически добавляются в воркспейс
- При отмене выделения товары удаляются из воркспейса
- Уведомления показывают количество товаров, добавленных/удаленных из воркспейса
- Автоматическое обновление подсветки товаров в воркспейсе
- Функция извлечения данных товара из карточек для работы с воркспейсом

## [2024-12-19] - Исправлены проблемы с воркспейсом

### Исправлено
- Улучшена функция извлечения SKU из карточек товаров - более точный поиск
- Добавлена валидация SKU - исключаются некорректные значения (например, "x")
- Улучшена обработка ошибок при добавлении/удалении товаров из воркспейса
- Добавлена автоматическая очистка некорректных данных из воркспейса при загрузке страницы
- Улучшены уведомления - показывают детальную статистику операций

### Улучшено
- Функция выделения товаров теперь показывает: добавлено, уже в воркспейсе, ошибок
- Функция отмены выделения показывает: удалено, не было в воркспейсе
- Добавлена функция `cleanupWorkspace()` для очистки некорректных данных
- Более надежное извлечение данных товара из карточек

## [2024-12-19] - Исправлена работа кнопок выделения при открытом воркспейсе

### Исправлено
- Кнопки "Выделить все" и "Отменить выбор" теперь работают при открытом воркспейсе
- Добавлена функция `reinitializeEventHandlers()` для повторной инициализации обработчиков событий
- Добавлены прямые обработчики `onclick` в HTML кнопки для надежности
- Автоматическая переинициализация обработчиков при открытии воркспейса

### Улучшено
- Обработчики событий для карточек товаров также переинициализируются в скопированном контенте
- Добавлено логирование процесса переинициализации для отладки
- Улучшена надежность работы кнопок в разных режимах отображения

## [2024-12-19] - Исправлена логика выделения и отмены товаров

### Исправлено
- Исправлен неправильный подсчет товаров при выделении/отмене
- Улучшена логика отмены выделения - теперь корректно удаляет только товары с текущей страницы
- Добавлена функция синхронизации визуального выделения с состоянием воркспейса
- Добавлено подробное логирование всех операций для отладки

### Улучшено
- Добавлена кнопка "Синхронизировать" для ручной синхронизации визуального выделения
- Автоматическая синхронизация при загрузке страницы и переинициализации обработчиков
- Более точные уведомления с указанием количества товаров на странице
- Улучшена обработка ошибок при работе с воркспейсом

## [2024-12-19] - Упрощена логика выделения товаров

### Исправлено
- Упрощена логика кнопок "Выделить все" и "Отменить выбор" - теперь работают только с визуальным выделением
- Убрана автоматическая работа с воркспейсом при выделении/отмене товаров
- Исправлен неправильный подсчет товаров - теперь учитываются только видимые карточки в каталоге
- Добавлено версионирование скриптов для принудительного обновления кэша браузера

### Улучшено
- Кнопки выделения теперь работают независимо от воркспейса
- Более точный подсчет видимых товаров на странице
- Упрощенные уведомления без лишней информации о воркспейсе
- Функция синхронизации остается для ручного управления состоянием

## [2024-12-19] - Восстановлена интеграция с воркспейсом

### Исправлено
- Восстановлена интеграция кнопок "Выделить все" и "Отменить выбор" с воркспейсом
- Кнопки снова добавляют/удаляют товары из воркспейса при выделении/отмене
- Сохранена улучшенная логика подсчета только видимых товаров на странице
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Оптимизация воркспейса

### Исправлено
- Убран избыточный логгинг из всех функций воркспейса
- Удалена зеленая кнопка "Синхронизировать" (не нужна)
- Упрощена логика выделения/отмены товаров

### Улучшено
- Создан новый роут `/workspace-cards` для получения массива карточек одним запросом
- Функции выделения/отмены теперь работают с массивами SKU вместо отдельных запросов
- Добавлена функция `getCatalogProducts()` для получения данных товаров из каталога
- Упрощена функция `renderWorkspaceProducts()` - теперь один запрос вместо множества
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Принудительная очистка воркспейса

### Исправлено
- Добавлена принудительная очистка контейнера воркспейса при отмене выделения
- Исправлена проблема с "застрявшими" товарами в UI воркспейса
- Добавлена принудительная очистка в функции removeProduct в SimpleWorkspaceManager
- Принудительное отображение состояния "пустой воркспейс" при очистке

### Улучшено
- Более агрессивная очистка воркспейса с принудительным удалением DOM элементов
- Принудительное обновление всех элементов UI воркспейса
- Подробное логирование процесса очистки для отладки
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Оптимизация воркспейса

### Исправлено
- Убран избыточный логгинг из всех функций воркспейса
- Удалена зеленая кнопка "Синхронизировать" (не нужна)
- Упрощена логика выделения/отмены товаров

### Улучшено
- Создан новый роут `/workspace-cards` для получения массива карточек одним запросом
- Функции выделения/отмены теперь работают с массивами SKU вместо отдельных запросов
- Добавлена функция `getCatalogProducts()` для получения данных товаров из каталога
- Упрощена функция `renderWorkspaceProducts()` - теперь один запрос вместо множества
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

### Улучшено
- Кнопки работают с воркспейсом, но учитывают только видимые товары
- Детальные уведомления показывают количество товаров, добавленных/удаленных из воркспейса
- Автоматическое обновление подсветки товаров в воркспейсе
- Подробное логирование всех операций для отладки

## [2024-12-19] - Исправлено обновление UI воркспейса

### Исправлено
- Добавлено принудительное обновление UI воркспейса после операций выделения/отмены
- Исправлена проблема с необновляющимся интерфейсом воркспейса после удаления товаров
- Добавлен вызов `renderWorkspaceProducts()` с задержкой для корректного обновления
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Оптимизация воркспейса

### Исправлено
- Убран избыточный логгинг из всех функций воркспейса
- Удалена зеленая кнопка "Синхронизировать" (не нужна)
- Упрощена логика выделения/отмены товаров

### Улучшено
- Создан новый роут `/workspace-cards` для получения массива карточек одним запросом
- Функции выделения/отмены теперь работают с массивами SKU вместо отдельных запросов
- Добавлена функция `getCatalogProducts()` для получения данных товаров из каталога
- Упрощена функция `renderWorkspaceProducts()` - теперь один запрос вместо множества
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Принудительная очистка воркспейса

### Исправлено
- Добавлена принудительная очистка контейнера воркспейса при отмене выделения
- Исправлена проблема с "застрявшими" товарами в UI воркспейса
- Добавлена принудительная очистка в функции removeProduct в SimpleWorkspaceManager
- Принудительное отображение состояния "пустой воркспейс" при очистке

### Улучшено
- Более агрессивная очистка воркспейса с принудительным удалением DOM элементов
- Принудительное обновление всех элементов UI воркспейса
- Подробное логирование процесса очистки для отладки
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Оптимизация воркспейса

### Исправлено
- Убран избыточный логгинг из всех функций воркспейса
- Удалена зеленая кнопка "Синхронизировать" (не нужна)
- Упрощена логика выделения/отмены товаров

### Улучшено
- Создан новый роут `/workspace-cards` для получения массива карточек одним запросом
- Функции выделения/отмены теперь работают с массивами SKU вместо отдельных запросов
- Добавлена функция `getCatalogProducts()` для получения данных товаров из каталога
- Упрощена функция `renderWorkspaceProducts()` - теперь один запрос вместо множества
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

### Улучшено
- UI воркспейса теперь корректно обновляется при всех операциях
- Синхронизация также обновляет UI воркспейса при необходимости
- Более надежное обновление интерфейса с задержкой для стабильности

## [2024-12-19] - Принудительная перерисовка воркспейса

### Исправлено
- Добавлена принудительная очистка контейнера воркспейса перед перерисовкой
- Исправлена проблема с "застрявшими" товарами в UI воркспейса
- Добавлено принудительное обновление счетчика воркспейса
- Добавлено принудительное отображение состояния "пустой воркспейс"

### Улучшено
- Более агрессивная перерисовка воркспейса с полной очисткой контейнера
- Принудительное обновление всех элементов UI воркспейса
- Подробное логирование процесса перерисовки для отладки
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Оптимизация воркспейса

### Исправлено
- Убран избыточный логгинг из всех функций воркспейса
- Удалена зеленая кнопка "Синхронизировать" (не нужна)
- Упрощена логика выделения/отмены товаров

### Улучшено
- Создан новый роут `/workspace-cards` для получения массива карточек одним запросом
- Функции выделения/отмены теперь работают с массивами SKU вместо отдельных запросов
- Добавлена функция `getCatalogProducts()` для получения данных товаров из каталога
- Упрощена функция `renderWorkspaceProducts()` - теперь один запрос вместо множества
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Принудительная очистка воркспейса

### Исправлено
- Добавлена принудительная очистка контейнера воркспейса при отмене выделения
- Исправлена проблема с "застрявшими" товарами в UI воркспейса
- Добавлена принудительная очистка в функции removeProduct в SimpleWorkspaceManager
- Принудительное отображение состояния "пустой воркспейс" при очистке

### Улучшено
- Более агрессивная очистка воркспейса с принудительным удалением DOM элементов
- Принудительное обновление всех элементов UI воркспейса
- Подробное логирование процесса очистки для отладки
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов

## [2024-12-19] - Оптимизация воркспейса

### Исправлено
- Убран избыточный логгинг из всех функций воркспейса
- Удалена зеленая кнопка "Синхронизировать" (не нужна)
- Упрощена логика выделения/отмены товаров

### Улучшено
- Создан новый роут `/workspace-cards` для получения массива карточек одним запросом
- Функции выделения/отмены теперь работают с массивами SKU вместо отдельных запросов
- Добавлена функция `getCatalogProducts()` для получения данных товаров из каталога
- Упрощена функция `renderWorkspaceProducts()` - теперь один запрос вместо множества
- Обновлена версия скриптов для принудительного обновления кэша

## [2024-12-20] - Исправление использования корутин в Celery задачах

### Исправлено
- Исправлена критическая проблема с использованием асинхронных методов в Celery задачах
- Добавлена синхронная версия метода `get_all_account_settings_sync()` в `ProductAllegroSyncService`
- Обновлена Celery задача `sync_allegro_stock_single_account_force()` для использования синхронного метода

### Улучшено
- Все Celery задачи теперь используют только синхронные версии методов сервиса синхронизации
- Устранена проблема с корутинами в синхронном контексте Celery
- Повышена стабильность работы системы синхронизации остатков

## [2024-12-20] - Добавление детального логирования для отладки синхронизации

### Добавлено
- Детальное логирование в функции `get_offers_from_microservice()` для отслеживания запросов к микросервису
- Логирование процесса фильтрации товаров по настройкам синхронизации в `sync_allegro_offers_batch()`
- Проверка наличия товаров в локальной базе данных с выводом детальной информации
- Логирование настроек синхронизации в `should_sync_product_sync()` для отладки
- Вывод списка SKU для синхронизации в принудительном режиме

### Улучшено
- Более подробная диагностика проблем с поиском товаров в Allegro
- Отслеживание каждого этапа синхронизации от локальной БД до микросервиса
- Детальная информация о настройках синхронизации для каждого товара

## [2024-12-20] - Исправление принудительного режима синхронизации

### Исправлено
- Исправлена критическая проблема с игнорированием флага синхронизации в принудительном режиме
- Добавлен параметр `force_mode` в функцию `sync_allegro_offers_batch()`
- В принудительном режиме теперь игнорируются настройки синхронизации и синхронизируются все товары из локальной БД
- Исправлена ошибка с атрибутом `id` в модели Product (используется `sku` как primary key)

### Улучшено
- Более четкое разделение логики между обычным и принудительным режимами синхронизации
- Детальное логирование принудительного режима для отладки
- Корректная передача параметра `force_mode=True` в принудительных задачах

## [2024-12-20] - Исправление структуры ответа микросервиса Allegro

### Исправлено
- Исправлена проблема с обработкой структуры ответа микросервиса Allegro
- Микросервис возвращает объект с полем `offers`, а не прямой массив офферов
- Добавлена проверка структуры ответа с поддержкой разных форматов
- Исправлена ошибка "Оффер не содержит external.id" из-за неправильной обработки данных

### Улучшено
- Более гибкая обработка ответов микросервиса
- Детальное логирование структуры ответа для отладки
- Поддержка как объектного, так и словарного формата ответов
